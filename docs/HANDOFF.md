# HANDOFF

> 세션을 끝낼 때 이 파일을 **덮어쓴다.** 다음 세션은 이것부터 읽는다.
> 파일은 항상 하나다 — 지난 내용은 `git log docs/HANDOFF.md` 에 남는다.

**갱신**: 2026-08-16 (2차) · **브랜치**: `main` · **부모 `992f848`+이 커밋** · **분석 `e580b54` (푸시됨)**

## 지금 상태

**한 세션에서 두 갈래가 진행됐다: TSMC v4.0 (기준선 교체, MAJOR) + 우버 스터디 v1.0 신설.**
도구 코드는 이번에도 한 줄도 안 건드렸다(`d523106` 그대로 배포 중). 분석 repo 커밋
5개(`3eb991c`…`e580b54`) 전부 푸시됨. 부모 저장소는 이 HANDOFF 커밋뿐이다.

### ① TSMC — 외부 리뷰 검증 → v3.2 → v4.0 (MAJOR)

외부 리뷰 지적 6건을 전부 코드로 재계산해 5건 수긍·1건 기각(아래 「정해진 것」).
이후 사용자 확정으로 **기준선을 2단계 유보 경로로 교체**했다:

- **적정 PBR 5.70 → 4.91** (ROE 28% · 유보 70%×5년 → 50%×10년 · N 15년). 유보 70% 는
  향후 ~5년의 실측(AI 증설)이고, 그 뒤는 재투자처 한계(성장 상한 g 14~16% · 현금
  ~$110B 적체)로 50% 정상화된다는 세계관. 전환 5년은 판단이다(fab 파이프라인 가시 범위)
- 파생 전부 재계산: 배수 1.93 → **2.24×** · 요구 N 22 → **약 29년** · 현재가 IRR 5.3 →
  **4.2%** · **사다리 3.5/5.7/9.1 → 3.3/4.9/6.8**(본 포지션 개시 $221 → **$190**) ·
  BEAR 2.94 · 공개 글 14곳 동기화. **판정(고평가·점검)·기각 조건 불변**
- v3.1 시나리오 표에 구 BPS $32.91 이 섞여 있던 사고(앵커 실패 유형의 **세 번째 재발**)를
  정정하고, **앵커 검사기를 파생 표까지 확장** — 시나리오 ADS 환산·조건부 IRR·공개 글
  시나리오 표를 앵커에서 재계산해 대조한다(고의 파손 3방향 시험 통과)
- APP.G 에 배당 현금 고정 유보(0.72→0.53) 병기. **손익분기 여유가 얇다** — 실측 비율
  0.896 vs 현금 고정 손익분기 0.906, 여유 0.010. 다음 다운사이클 저점 실측이 세게 나오면
  "괴리 확대" 방향이 뒤집힐 수 있는 거리다. 문서에 정직하게 병기돼 있다

### ② 우버 — 스터디 v1.0 신설 (`companies/uber/`)

TSMC 의 교훈(파이프라인 역행 — 적정가부터 시작했었다)대로 이번엔 **재료부터** 깔았다.
ultracode 워크플로 14에이전트: 8차원 조사 → EDGAR 원문 적대적 검증(대조 71건·정정
38건) → 템플릿 6장 집필 → 이중 리뷰 4건 반영. 561줄 · 판단 문장 0 · 전 수치 신뢰도 라벨.

- **uber-분석.md 는 미착수** — 다음 단계는 TOOL 01 → 02 → 03 순서다
- 못 채운 13건은 **스터디 말미 「미확인 잔여」 목록**에 있다. 재료 원장(R-*.md 스크래치)은
  세션과 함께 사라지므로 그 목록이 유일한 인수인계다
- 검증에서 배운 것: 핵심 시계열(GB·매출·조정 EBITDA·순이익 2017~2025)은 전 연도 원문
  일치했고 틀린 건 서술 층이었다(부채 상품 혼동·발행액 이중계상·이연법인세 부호 반대) —
  **표보다 표를 둘러싼 문장을 의심할 것**

## 알아야 할 것

- **⚠ 가격 앵커 규칙 그대로** — 주가와 자본은 같은 시점. 검사기(`verify-tsmc-anchor.js`)가
  이제 앵커 산술·시점 간격(100일)·**시나리오 표·IRR 표·공개 글 동기화**까지 잡는다.
  분기 갱신 = 앵커 표 6줄 + 파생 표를 본문 병기 공식으로 재계산. 빠뜨리면 검사가 잡는다
- **⚠ 세션 사용량 한도와 워크플로** — 이번에 워크플로가 한도에 두 번 걸려 세 번에 나눠
  완주했다. 실패해도 `Workflow({scriptPath, resumeFromRunId})` 로 재개하면 완료분은 캐시
  복원된다. 리셋 시각은 에러 메시지에 적혀 있다
- **EDGAR 재취득법** — User-Agent 에 연락처 이메일 필수. TSMC CIK `0001046179` ·
  **Uber CIK `1543151`** · Fundsmith 13F CIK `0001569205`(다음 13F 2026년 11월 중순)
- **켈리 도구에 TSMC 를 넣으면 여전히 보류가 맞다** — 세 목표가가 전부 현재가 아래
  (v4.0 에서 더 내려갔다). $ 환산은 `목표가 = 현재가 × (목표PBR/11.01)`. BPS 곱하기 금지
- 도구 줄 번호 표 · `-25308` 키체인 경고(무해) · 20-F vs 6-K 자본 1.2% 차(미결 ⑬) ·
  ADS=보통주 5주 — 직전 판 그대로 유효(`git log docs/HANDOFF.md` 의 `992f848` 판 참조)

## 손대지 말 것

- **기존 항목 전부 승계** — `.gitignore:3` 의 `docs/analysis/` 줄 · 분석 자료를 부모
  저장소에 커밋 금지 · `<!-- ANCHOR -->` 마커와 행 이름 · APP.G 에 절대 PBR 금지 ·
  부록 글자 재사용 금지 · `_template` 판정 규칙 사본 임의 정리 금지 · 도구 관련 전부 ·
  `prob` 을 "승률"로 되부르기 금지. 상세는 `992f848` 판
- **신규 ⑴ TSMC 참고 상한(14.25)은 정률 유보를 유지한다** — 물리 제약 위반을 보여주는
  경계 표시가 그 행의 일이라 2단계 유보를 적용하지 않는다(본문에 명시)
- **신규 ⑵ 우버 스터디는 재료 문서다** — 판단(싸다/비싸다/좋다)을 넣지 말 것. 판단은
  `uber-분석.md` 가 생기면 거기로 간다

## 이번에 정해진 것 — 다시 묻지 말 것

- **TSMC 기준선 = 2단계 유보 4.91 (2026-08-16 사용자 확정).** "개선"이라며 정률 5.70 으로
  되돌리면 안 된다. 근거·경위는 Mode 1 · APP.C · 히스토리 v4.0 행에 있다
- **리뷰의 "터미널 숨은 낙관" 논거는 기각** — "회계 ROE 10% = 경제적 10% 초과"라는 전제가
  APP.G 자신의 결론(5년 상각은 회계 ROE 를 부풀린다)과 도치돼 있다. 재론하려면 히스토리
  v3.2 행의 기각 사유부터 읽을 것
- 이전 확정 승계: 근무처 노출 히스토리 유지(filter-repo 기각) · HANDOFF 공개 저장소 유지 ·
  공개 글 파일명 유지

## 미결 — 다음 세션이 이어받을 것

- **우버 분석 착수** (TOOL 01 → 02 → 03) — 스터디 말미 미확인 13건을 병행 해소.
  반도체 밖 종목이라 §P8(다음 종목 권장)과 부합하나 3중 집중 심사는 별도로 밟을 것
- TSMC 미결 큐(`tsmc-분석.md` 끝): ① 반증 가능 가설 초안(문턱 4.9 로 갱신됨) 채택 —
  사용자 몫 · ⑤ 중앙값 4.81 지위 — **기준선 4.91 과 사실상 겹쳐져 실익 자체 재평가** ·
  ⑧ 스커틀벗(사용자 몫, «범위 안» 승급의 유일한 길) · ⑨ 인버전 경로 6 번역 · ⑩ 피셔
  F07·F08 1차 자료 · ⑪ IRR 엔진 종료 가정 · ⑫ 시세 재취득 절차 명문화만 남음 · ⑬ 자본
  1.2% 차 · ⑦ 13F 관찰(11월)
- 도구 쪽 승계(변동 없음): `index_premium.html:575` 문구 정정(다음 도구 수정 때 연동 3파일) ·
  폰 실기 확인 3건 · `kelly-pf-*` 폐기 키 · 기타 `992f848` 판 그대로

## 검증 상태

- 분석 repo 검사 3종 전부 통과 — 앵커 검사기는 v4.0 값 기준으로 파생 표까지,
  상각 검사기는 정률 5.70·2단계 4.91 이중 검증
- 우버 스터디: 워크플로 안에서 EDGAR 대조 71건 + 규약·내용 이중 리뷰. **세션 주체의
  자체 재검증은 규약 수준만**(판단 문장·라벨·구조) — 수치 표본 재검산은 분석 착수 때 할 것
- 안 함: `node test_pipeline.js`(도구 무변경) · 자체 검사 · 폰 실기

## 되돌리는 법

분석 repo (`docs/analysis` 안, 전부 푸시됨 — revert 도 푸시 필요):

- `git revert e580b54` — 우버 스터디 제거
- `git revert 39dde59` — v4.0 → v3.2. **⚠ 사용자 확정을 뒤집는 것이므로 재확인 없이 금지**
- `git revert 4511f55 8d94484 3eb991c` — 리뷰 반영 전(v3.1)으로. 검사기 확장까지 같이
  사라지므로 개별 revert 권장하지 않음

부모 저장소: 이 HANDOFF 커밋 하나. 도구 코드·`.gitignore` 무변경.
