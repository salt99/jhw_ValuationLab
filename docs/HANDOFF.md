# HANDOFF

> 세션을 끝낼 때 이 파일을 **덮어쓴다.** 다음 세션은 이것부터 읽는다.
> 파일은 항상 하나다 — 지난 내용은 `git log docs/HANDOFF.md` 에 남는다.

**갱신**: 2026-08-14 · **브랜치**: `main` · **배포됨 (`9f49036`)**

## 지금 상태

**미결 정리 세션이었다.** `f233930..9f49036` 푸시 완료. 새 기능 없음, 계산 로직 변경 없음.
라이브: `https://salt99.github.io/jhw_ValuationLab/index_kelly.html`

1. **화면 문구를 3-시나리오에 맞췄다** (`f309de4`) — `index_kelly.html` 11곳 + 허브 1곳
2. **README·PRD·CLAUDE.md 드리프트 해소** (`9f49036`)
3. **병합된 브랜치 3개 삭제** (로컬+원격) — 커밋 없음

### 1. 문구 — "승률"은 오기가 아니라 거짓말이었다

이게 이번 세션의 본론이다. D20 에서 `prob` 의 의미가 **승률 → P(Bull)** 로 바뀌었는데
(§7-D21) 화면은 계속 "승률"이라 불렀다. 사용자가 그 칸에 승률 55 를 넣으면 도구는
"상단 도달 확률 55%" 로 읽고 나머지 45% 를 Base·Bear 로 쪼갠다. **예외도 경고도 없이
의도와 다른 배분이 나온다.** 3-시나리오 확장(§7-D20) 이후 줄곧 이 상태였다.

`Base확률`(`index_kelly.html:2601`)이 이미 쓰이고 있었으므로 짝을 맞춰 **`상단확률`** 로
통일했다. 바꾼 자리: 태그라인·설명 노트 공식·재평가 모달 3곳·최근 재평가 줄·📊 버튼
title·논지 훼손 배너·파이프라인 편입 상태줄·재평가 델타·주석 1곳.

설명 노트의 켈리 공식도 2-시나리오 `f = p/a − q/b` 였다. `kelly3` 가 실제로 푸는
**`E[ln(1+f·r)]` 최대화**로 바꿨다.

허브(`index.html`)의 **"하프 켈리 안전마진 포함"** 은 D22 로 `HALF` 를 지우면서 틀린
문장이 됐다. 태그 `100% 정규화` 도 마찬가지 — D23 이후 총노출은 100% 정규화가 아니라
종목 수에 따른 **30/60/90/100 사다리**다. 둘 다 교체했다.

### 2. 문서

- `README.md` — v1 켈리("포트폴리오 배분기", "하프 켈리 안전마진 포함", "하프 켈리 이하
  권장")를 기술 중이었다. 셋 다 D22 로 틀린 문장. 공식도 2-시나리오였다.
  "`index_main.html` 을 `index.html` 로 rename 하세요" 섹션은 **이미 완료된 과거 절차**라
  통째로 걷어냈다 — 남겨두면 다음 사람이 안 끝난 일로 읽는다.
- `PRD.md` §8 드리프트 항목을 해소로 표시하고 이번에 발견한 화면 문구 드리프트를 기록.
  §9 백로그와 `CLAUDE.md` 백로그에서 README 줄 삭제.

### 3. 브랜치

셋 다 `main` 에 병합 확인(`git rev-list --count main..<branch>` = 0)하고 지웠다.
**HANDOFF 에는 1개만 적혀 있었지만 실제로는 3개였다.**

| 브랜치 | 팁 해시 |
|---|---|
| `feat/kelly-three-scenario` | `4eb8e8c` |
| `feat/rejection-ledger` | `0bc0577` |
| `feat/reviewing-stage-fields` | `320317c` |

되살리려면 `git branch <이름> <해시>`. 해시는 `main` 에서 도달 가능하므로 GC 되지 않는다.

## 알아야 할 것

- **⚠ 이전 HANDOFF 의 줄 참조 5곳이 stale 이었다.** 이번에 바로잡았다(아래 표는 현재값).
  **인용 전에 `grep -n` 으로 확인할 것** — 직전 커밋 `f233930` 도 같은 문제를 고친 것이다.

  | 대상 | 현재 줄 |
  |---|---|
  | `compute()` | `index_kelly.html:988` |
  | `MAX_SINGLE` / `THIN_DOWNSIDE` | `:929` / `:928` |
  | `colorOf` 맵 | `:1299` |
  | `thesisWorsened` 의 `prevPBase` 가드 | `:1417` |
  | `load()` 안의 `advanceStartQ` 호출 | `:1734` |
  | `readProb`(죽은 코드) | `:1008` |

- **⚠ 설계 문서 3곳이 코드와 어긋난다 — 소급 수정하지 않았다.**
  `docs/superpowers/specs/2026-08-13-kelly-three-scenario-design.md` 의 `:106`
  "`MAX_SINGLE`(40%)", `:121` "Half-Kelly 는 총노출 결정에만 관여한다", `:129`
  "`equityScale = min(posSum,1)×0.5`" 는 **전부 D22·D23 으로 대체됐다.** 날짜 붙은 설계
  문서는 그 시점의 판단 기록이라 고치지 않는 게 맞다고 판단했으나, 안 적어두면 다음 세션이
  스펙을 읽고 40%·하프 켈리를 현재 사실로 믿는다. **정본은 PRD §4.4 와 코드다.**
- **엄밀한 의미의 "제약 하 켈리"는 아니다.** 진짜 제약 하 log-optimal 이라면 `w ≤ 0.30`
  을 걸고 다시 최적화해야 하고, 그러면 남는 여력이 채권이 아니라 **다른 주식으로** 간다.
  지금은 "켈리를 푼 다음 자른다"이고 구 재분배가 오히려 그쪽에 가까웠다. 다만 도구는 애초에
  진짜 다자산 켈리가 아니다 — 설계 문서 §4 "종목 간 상관관계를 무시한다. 진짜 다자산
  켈리는 공분산이 필요하다". 각 종목 `f` 를 따로 풀어 정규화하는 것 자체가 휴리스틱이라
  "제약 하 최적화"를 따질 기반이 없다. **상한의 역할은 켈리를 정확하게 만드는 게 아니라
  켈리가 못 보는 것(왼쪽 꼬리·상관관계)을 상쇄하는 것**이고(설계 문서 §3), 그렇다면 잘라낸
  초과분을 역시 켈리로 추정된 다른 종목에 넘기는 건 과대평가를 옮기기만 하는 셈이다.
  이게 채권으로 보내기로 한 이유다. 되돌리려는 다음 세션은 이 문단을 먼저 읽을 것.
- **채권이 0% 가 되면 8분기 분할매수의 "매수 대기 실탄"이 사라진다.** 분할 속도가 남은
  분기 수로만 통제된다. 상한 덕에 종목이 4개 미만이면 채권이 남으므로 초기에는 문제가
  덜하지만, 4종목 이상에서 켈리가 고르면 채권 0% 가 된다.
- **PBR로 입력하면 켈리 값(`f`)은 맞지만 주수가 틀린다.** 켈리는 비율이라 스케일 불변이지만,
  매수 금액을 주가로 나누는 주수 계산은 스케일에 의존한다. TSMC처럼 분석 문서가 PBR
  단위면 **BPS로 환산한 달러(`$`) 값**을 입력할 것. 자동 환산은 만들지 않았다(BPS는 분기마다
  변한다 — 안 만드는 것, PRD §4.4/설계 문서 참조).
- **종목마다 시나리오 도달 기간 관점을 맞춰야 `f` 비교가 유효하다.** 도구는 기간을 묻지
  않는다 — 3년 관점 종목과 15년 관점 종목을 같은 `posSum`에 넣으면 상대 배분이 왜곡된다.
  연율화하지 않는 것도 의도다(설계 문서 «켈리 전제와 한계» §1). **풀 켈리로 바뀌면서 이
  왜곡의 대가가 2배가 됐다.**
- **입력 레이아웃이 스펙 목업과 다르게 렌더된다.** `.prob-in{width:64px}` 가
  `.fld input{width:100%}` 에 밀려(특정성 0,1,0 vs 0,1,1) 목표가·확률이 나란히가 아니라
  **위아래로 쌓인 동일 너비 입력 두 칸**으로 나온다. 사용자가 폰에서 보고 "이상없음"
  했으므로 실사용상 문제는 없다고 판단됐다 — 고치라는 요청은 없었다.
- **설계 문서 §"모든 rᵢ ≤ 0" 행과 실제 동작이 어긋난다.** 설계 문서는 "f < 0 정상 반환 →
  §7-D6 배분 제외"라고 적었지만, 실측으로는 `kellyOf` 가 `err:'시나리오가 퇴화했습니다'`
  를 반환한다 — 세 rᵢ가 전부 음수면 `f→−∞` 로 목적함수가 무계라 유효 극점이 없기 때문.
  배분에서 빠지는 결과는 같고 에러 문구만 다르다. 알고리즘은 안 건드렸다.
- **`readProb()` 는 호출처 0인 죽은 코드다**(`index_kelly.html:1008`). 기존 코드라
  삭제하지 않았다(CLAUDE.md §3). 지울 거면 별도 커밋으로. `readProbs()`(`:1012`)가
  실제로 쓰이는 쪽이니 헷갈리지 말 것.
- **`test_pipeline.js:252, 380, 386` 의 `f = p/a − q/b` 는 stale 이 아니다.** 3-시나리오를
  2-결과로 축퇴시켰을 때 옛 공식과 일치하는지 검증하는 앵커다(§7-D20 축퇴 성질).
  문구 드리프트로 착각해 지우면 롤백 경로의 검증이 사라진다.

## 손대지 말 것

- **화면에서 `prob` 을 다시 "승률"로 부르지 말 것.** v5 에서 `prob` = P(Bull) 이고
  나머지는 `pBase` 와 `pBear`(=1−prob−pBase)로 갈린다. "승률"로 되돌리면 사용자가
  넣는 숫자의 의미가 조용히 달라진다 — **예외가 안 나고 배분만 틀린다.** 화면 표기는
  `상단확률`·`Base확률` 한 쌍으로 유지한다.
- **`kellyOf` 의 선언 형태**(`function kellyOf(h){`) — `test_pipeline.js` 의 `grab()` 이
  중괄호 균형으로 이 형태를 찾는다. 화살표 함수로 바꾸면 테스트가 통째로 죽는다.
  `compute`·`kelly3`·`livePlanHoldings`·`isThesisReject` 도 같다.
- **`test_pipeline.js:31` 의 `constOf(...)` 목록** — 이제 `MAX_SINGLE`·`THIN_DOWNSIDE`
  둘뿐이다. `HALF` 를 지우면서 함께 뺐다. 여기 없는 상수를 `compute()` 에서 참조하면
  테스트가 `ReferenceError` 로 죽는다.
- **`kelly3` 의 `α≈0 ∧ β≈0` 가드**(`if(Math.abs(A)<1e-12){ if(Math.abs(B)<1e-12) return null; ...}`).
  `p_base=1` 처럼 "확실히 제자리"인 입력에서 0/0을 막는 유일한 장치다.
- **`thinDown` 이 `Math.abs(Math.min(...rs))` 기준인 것.** 하단 칸(`rs[2]`)이 아니라 세
  시나리오 중 최솟값 기준이어야 한다 — 순서 검증이 있는 한 수학적으로는 `rs[2]` 와
  같지만(돌연변이 C로 확인, 방어적 코드), 순서 검증이 미래에 느슨해지면 마지막 방어선이 된다.
- **`kellyOf` 의 순서 검증**(`if(!(rs[0]>=rs[1] && rs[1]>=rs[2])) return {...err:'상단 ≥ Base ≥ 하단이 아닙니다'}`).
  Base·Bear를 바꿔 적어도 수학은 조용히 돌아간다 — 화면의 "Base" 라벨이 거짓말하는 걸 막는
  **유일한** 장치다. 돌연변이 테스트로 확인: 이 줄을 지우면 딱 그 검증 하나만 FAIL한다.
- **`thesisWorsened` 의 `prevPBase!==undefined` 가드**(`index_kelly.html:1417`).
  3-시나리오 이전 원장 항목에는 `prevPBase` 가 없어, 가드를 빼면 `NaN` 비교가 조용히
  false 가 되거나 레거시 항목을 오분류한다.
- **마이그레이션에 기본값을 채우지 말 것.** v4→v5는 스냅샷 버전 표기만 갱신하고 `base` 는
  비워둔다. 추정값을 채우면 "아무도 생각한 적 없는 숫자"가 조용히 배분을 움직인다 —
  3-시나리오 확장 전체의 forcing function 이 무의미해진다.
- **`compute()` 의 `rel = w/equityScale` 한 줄**(`index_kelly.html:988` 의 `compute()` 안).
  렌더 4곳이 `rel × equityScale` 로 최종비중을 복원한다. `rel` 을 켈리 몫(`f/posSum`)으로
  되돌리면 상한이 걸린 종목의 화면 숫자가 조용히 커진다 — 테스트
  `상한 — rel × equityScale 항등식` 그룹이 이걸 지킨다.
- **종목 색을 `colorOf` 맵(종목 id 기준)으로 매기는 것**(`index_kelly.html:1299`).
  배분바는 `pos`(켈리 양수만), 카드는 `rows`(보류·음수 포함)를 돌기 때문에 각자 인덱스로
  매기면 보류 종목이 끼는 순간 그 뒤가 통째로 밀린다. **예외가 안 나고 화면만 조용히
  거짓말한다.** 자체 검사 25번이 이걸 지키고, 인덱스 매핑으로 되돌리는 돌연변이가 2건을
  FAIL 시키는 것까지 확인했다.
- **`--demo` 픽스처 값**(`docs/superpowers/build-selfcheck.js`). 고정 픽스처라 값이 바뀌면
  화면 비교의 기준이 사라진다(§7-D25). 바꿔야 하면 그 파일 주석의 기대 배분표와
  체크리스트 2단계의 표를 **함께** 갱신할 것 — 세 곳이 같은 숫자를 말해야 한다.
  **단 `startQ` 만은 고정값이 아니라 `currentQGuess()` 다** — 지난 분기를 박으면 §7-D26
  동작과 어긋나 "8월인데 Q1 시작" 화면이 다시 나온다.
- **`load()` 안의 `advanceStartQ` 호출**(`index_kelly.html:1734`). 이 호출이 빠져도
  `test_pipeline.js` 는 **129건 전부 통과한다** — 순수 함수는 멀쩡하기 때문이다. 실제로
  돌연변이로 확인했고, 자체 검사 26번 3건만 FAIL 했다. 단위 검사가 지켜주지 않는 자리다.

## 미결 — 다음 세션이 이어받을 것

### 폰이 있어야 하는 것

1. **폰 실기 확인 (누적 2건)** — ⑴ 사이징: 3종목이면 `주식 목표 90% · 채권 10%` 근처가
   나와야 한다(켈리가 고를 때). 배분바에 채권 구간이 생기고, 상한에 걸린 종목엔
   `상한 30%` 태그와 `초과분은 다른 종목이 아니라 채권으로 갔습니다` 경고가 뜬다.
   **종목 색이 실제 폰 패널·조명에서 구분되는지**도 여기서만 확인된다 — ΔE 38 이상으로
   계산했지만 계산은 화면 밝기를 모른다. ⑵ **이번 문구 변경**: 확률 3칸이 "상단확률"로
   불리는지, 재평가 모달·📊 버튼 title·켈리 공식 노트가 3-시나리오 표현인지.
   체크리스트 2단계에 항목이 있다.
2. **각 보유 종목의 Base 채우기** — 폰에서 `✎ 수정`으로 하나씩. 3종목은 채워진 것으로
   확인됐다(사용자 "3개 종목추가했는데").
3. **밀린 폰 검증**(검토 필드, §7-D19) — 진행 체크박스 줄바꿈, 계기 줄 스타일,
   `✎ 메모` 모달 스타일. 체크리스트: `docs/superpowers/browser-checklist-rejection-ledger.md`.

### 결정이 필요한 것

4. **`kelly-pf-*` 폐기 키 정리** (PRD §9) — 코드엔 **이미 한 곳도 없다.** PRD §5 키
   레지스트리와 CLAUDE.md 백로그에만 "폐기 키"로 남아있다. 사용자에게 세 안을 제시했고
   **아직 고르지 않았다**: ⑷a `load()` 에서 조용히 `removeItem`(3줄) · ⑷b 안 함(그 키가
   실제 폰에 남아있는지 확인된 적이 없다 — P6 대로면 이쪽) · ⑷c 정리 유틸 UI(과공학).
5. **목표 종목 수 5개 / 방어선 3개** (미확정) — 근거: 5종목 균등이 각 20% 라 상한 30% 가
   확신 배율 1.5배를 남긴다. 다음 종목은 **반도체 밖에서** 찾는 게 좋다 — 인적자본
   (반도체 커리어)이 이미 1칸이다(PRD §P8). 도구는 상관관계를 못 보므로 후보 파이프라인의
   `factor`(팩터 중복) 기각이 그 판단을 사람에게 맡겨둔 자리다. 정하면 PRD §7 에 기록.
6. **최근 재평가 줄에 Base 를 넣을지**(`index_kelly.html:1344`). 지금은
   `상단 X · 하단 Y · 상단확률 Z%` 만 보여준다. 거짓은 아니고 불완전한 것이라 문구 수정
   범위를 넘는다고 보고 안 건드렸다. 사용자에게 물었고 답 없음.
7. **허브 h2 가 아직 "켈리 포트폴리오 배분기"**(`index.html:119`). CLAUDE.md 는
   "켈리 · 분할매수 플래너"라 부른다(`index_kelly.html` 의 `<h1>` 도 그렇다). 라이브
   사이트에 보이는 도구 제목이라 임의로 안 바꿨다. 사용자에게 물었고 답 없음.

### 리서치 (P1 — 출처 검증 non-negotiable)

8. **피셔의 분산 관련 원문 확인** — 과분산을 반대하고 기업 유형별 최소 보유 수를 제시한
   것까지는 확실하나 **정확한 숫자는 확인되지 않았다.** 오귀속 3건 이력이 있으므로,
   확인 전에는 PRD·화면 어디에도 숫자를 적지 않는다.
9. **프리미엄 체크리스트 11요소의 원저 서지 확정** (PRD §9).

## 검증 상태

이번 세션 변경분(문구·문서)에 대해:

- 끝남: `node test_pipeline.js` — **129 pass** (변경 전과 동일, 회귀 없음)
- 끝남: 자체 검사(headless Chrome, `_selfcheck.html`) — **전부 통과 · 137건**
  (136 PASS + 1 SKIP · SKIP은 저장 데이터가 없어서, 정상). 직전 세션 기준선과 일치
- 끝남: `git diff` 육안 — **hunk 전부 문자열·주석**. 로직 변경 0건
- 끝남: 배포 5개 파일 전수 grep — `승률`·`하프 켈리`·`p/a − q/b` **0건**
- 안 함: **폰 실기** — 위 미결 1번

> **주의**: `grep -c FAIL dom.html` 로 자체 검사 결과를 세면 **스크립트 소스의 문자열
> 리터럴 `'FAIL'` 4개가 잡힌다.** 이번에 실제로 헛짚었다. 결과는 `#selfcheck-panel`
> div 의 헤더(`전부 통과 · N건` 또는 `FAIL N건 / 전체 M건`)를 읽어야 한다.

### 자체 검사 돌리는 법 (GUI 없는 세션용)

```bash
node docs/superpowers/build-selfcheck.js          # _selfcheck.html 생성 (gitignored)
python3 -m http.server 8731 &                     # file:// 이 아니라 http 로 띄운다
("/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new \
   --disable-gpu --no-sandbox --dump-dom http://localhost:8731/_selfcheck.html > dom.html &)
sleep 14; pkill -f dump-dom                       # Chrome 151 은 dump-dom 후 안 죽는다
rm -f _selfcheck.html; pkill -f "http.server 8731"
```

`--virtual-time-budget` 은 오히려 악화시킨다. 뷰포트는 `--window-size` 와 무관하게 500px 로
고정되므로 **실제 360px 폰 폭은 이 방법으로 검증되지 않는다.**

## 되돌리는 법

### 이번 세션 되돌리기 — 데이터 안전

문구·문서만 바뀌었고 저장 형식도 계산도 그대로다. `git revert 9f49036 f309de4` 로 충분하다.
다만 **되돌리면 "승률" 거짓말이 화면에 돌아온다** — 위 «손대지 말 것» 첫 항목을 먼저 읽을 것.

지운 브랜치는 `git branch feat/kelly-three-scenario 4eb8e8c` 식으로 되살린다(위 표 참조).

### 사이징 변경 되돌리기 — 데이터 안전

**저장 데이터 형식이 안 바뀌었으므로 코드만 되돌리면 된다.** `git revert` 로 충분하다.

- 상한만: `git revert 5079bfa` — 40% 재분배 방식으로 돌아간다(2종목 구멍도 함께 돌아온다)
- 풀 켈리까지: `git revert 5079bfa 84d0a9b` — 하프 켈리·40% 재분배로 완전 복귀
- **상한 수치만 바꾸고 싶으면** `MAX_SINGLE`(`index_kelly.html:929`) 한 줄이다. 다만
  `test_pipeline.js` 의 `상한 —` 세 그룹이 0.30 을 상수로 박아두었으므로 함께 고쳐야
  한다. 그게 의도다 — 사이징이 조용히 바뀌면 안 된다.

### ⚠ 3-시나리오까지 되돌리기 — 데이터가 안전하지 않다

**`prob` 필드의 의미가 바뀌었다.** v4에서는 "승률"(나머지 1−p가 전부 하단)이고, v5에서는
"P(Bull)"(나머지가 Base와 Bear로 나뉜다). 필드 이름과 타입이 같아서 옛 빌드가 **에러 없이
읽고 조용히 다르게 해석한다.**

실측 — 현재가 100 · Bull 150(25%) · Base 120(50%) · Bear 70(25%) 종목:

| 빌드 | f | 결과 |
|---|---|---|
| 새(3-시나리오) | **1.6085** | 배분에 포함 |
| 옛(같은 데이터) | **−0.6667** | **음수 켈리로 배분에서 제외** |

종목이 계획에서 통째로 사라지는데 경고가 없다. 되돌리려면 순서가 있다:

1. 폰에서 `⬇ 내보내기`로 v5 백업을 받아둔다 (나중에 다시 켤 때 필요하다)
2. 코드를 revert 해서 배포한다
3. **v5 이전 백업으로 `⬆ 가져오기`** 하거나, 각 종목의 `prob` 을 옛 의미(승률)로 손으로
   고친다. 안 하면 종목들이 배분에서 사라진 채로 남는다

계산 수준의 롤백(코드는 새 빌드, 값만 옛 공식으로)은 `kelly3` 에 `p_base=0, r_base=0` 을
넘기면 정확히 같은 값이 나온다 — 축퇴 성질이 그 경로를 겸한다(PRD §7-D20).

**교훈**(§7-D21): 필드를 **추가**하는 것과 기존 필드의 **의미**를 바꾸는 것은 다르다.
후자는 스냅샷 버전만으로 방어되지 않는다 — 옛 빌드는 버전을 보고 거부하지 않고 그냥 읽는다.
이번 세션의 문구 드리프트가 그 교훈의 **두 번째 청구서**였다. 의미를 바꾸면 저장 포맷만이
아니라 **그 필드를 부르는 화면 이름도 전부 따라와야 한다.**
