# HANDOFF

> 세션을 끝낼 때 이 파일을 **덮어쓴다.** 다음 세션은 이것부터 읽는다.
> 파일은 항상 하나다 — 지난 내용은 `git log docs/HANDOFF.md` 에 남는다.

**갱신**: 2026-08-24 · **브랜치**: `main` · **부모 `706230f`+이 커밋** · **분석 `68f02f6` (푸시됨)**

## 지금 상태

**이번 세션도 도구 코드를 한 줄도 안 고쳤다.** 한 일은 하나다 —
**Coca-Cola(KO) 문서 셋을 새로 만들었다.** `68f02f6` 로 커밋·푸시됨.

| 파일 | 무엇 | 분량 |
|---|---|---|
| `companies/coca-cola/coca-cola-스터디.md` | 본문 6장 · v1.0 | 1,215줄 · 66KB |
| `companies/coca-cola/coca-cola-히스토리.md` | 개정 이력 + 함정 3건 | 48줄 |

**`coca-cola-분석.md` 는 없다.** 판정·밸류에이션·적정가는 아직 시작도 안 했다.
이제 종목 폴더는 넷이다 — tsmc · uber · netflix · **coca-cola**.

### 어떻게 썼나

**SEC EDGAR 원문 6건에서만 썼다.** KO 10-K FY2025 · 10-Q 1Q26·2Q26 · DEF 14A 2026 ·
**PepsiCo 10-K FY2025** · **Keurig Dr Pepper 10-K FY2025**.

경쟁사 두 건을 추가로 받은 이유는 **점유율이 어느 회사 공시에도 없기 때문**이다.
경쟁 구도를 기억으로 채우면 P1 위반이라, 대신 세 회사 손익을 같은 잣대로 놓았다.
그 결과가 이 문서의 가장 큰 발견이다 — **마진 격차의 주된 설명은 브랜드가 아니라
사업 모델(원액이냐 병입이냐)이고, 그것이 펩시 자신의 세그먼트로 증명된다**:
PEP 해외 원액(IB Franchise) 35.4% vs PEP 북미 병입(PBNA) 3.9%. 한 회사 안에서 9배다.

**템플릿(`_template/`)이 아니라 netflix 규격을 따랐다.** 템플릿은 5장 시점 산물이라
CH.06(경영진·자본배분)이 없고, 그러면 TOOL 02 의 정직성 게이트(F15)와 피셔 서브 7개를
답할 곳이 사라진다. **tsmc 스터디가 지금 그 부채를 지고 있다**(v1.1, CH.06 없음).

### 가독성 — 처음부터 규칙대로 썼다

| 문서 | 200자↑ 산문줄 | 산문 최장 | 연속 산문 최대 |
|---|---:|---:|---:|
| **coca-cola-스터디** | **0** | **139자** | **5줄** |
| netflix-스터디 | 3 | 245자 | — |
| tsmc-스터디 | 0 | 186자 | — |
| uber-스터디 | 0 | 194자 | — |

12줄을 넘는 덩어리 2건은 **둘 다 순수 표**(산문 0줄)다 — 표는 빈 줄로 가르면 깨진다.

## 알아야 할 것

**이번 세션 신규 — 모르면 coca-cola 문서를 망가뜨린다:**

- **⚠ 스터디의 `약칭:줄번호` 인용은 세션 한정이다.**
  `10K:1729` 는 EDGAR HTML 을 **이 세션에서 텍스트로 변환한 결과** 기준이다.
  다시 받아 변환하면 **어긋난다.** 검증할 때는 줄번호가 아니라 **인용문 자체로 원문을 검색**해라.
  이 경고는 스터디 「원자료」 절과 히스토리 양쪽에 박아 뒀다.
- **⚠ KO 세그먼트 마진은 기준이 두 개고, 밝히지 않으면 조용히 틀린다.**
  10-K 자체 영업이익률 표(`10K:839-846`)는 **제3자 매출** 기준이고,
  Note 20 세그먼트 표(`10K:2703`)는 **내부거래 포함 총매출**을 준다.
  EMEA 는 두 기준에서 **39.7% vs 37.3%** 로 갈린다.
  스터디 CH.04 는 총매출 기준으로 통일하고 그 사실을 본문에 명시했다.
  **기준을 안 밝힌 세그먼트 마진 숫자를 새로 넣지 마라.**
- **⚠ KO 의 FY2024·FY2025 영업현금흐름을 그대로 쓰면 안 된다.**
  서로 다른 두 개의 거대한 일회성 유출이 각각 끼어 있다 —
  **FY2024 는 IRS 예치금 $6.0B, FY2025 는 fairlife 마일스톤 $6.1B.**
  표면 FCF 로는 두 해 다 배당을 못 갚는 것처럼 보이지만 조정하면 커버율 128%·130% 다.
  1H26 OCF $7,543M(vs 1H25 −$1,391M)이 이 해석을 뒷받침한다.
- **⚠ KO 분기 물량은 두 지표의 기준이 다르다.**
  unit case 는 **일평균 판매** 기준, 원액 물량은 **기간 총액** 기준이다.
  **1Q26 은 1Q25 보다 6일 길었고 4Q26 은 6일 짧다**(`10Q2:1088`).
  4분기 원액 물량이 눌려 보일 것을 미리 알고 있어야 한다.
- **⚠ IRS 항소심 판결이 안 나왔다 — 나오면 문서를 고쳐야 한다.**
  제11연방항소법원이 **2026-06-25 변론을 마쳤고** 2026-08-24 현재 판결이 없다(`10Q2:1300`).
  노출은 납부한 $6.0B + 2010~2025년 **약 $14B** + 분기당 약 $450M 누적.
  **이산적 사건이라 분석 문서의 기각 조건으로 바로 번역된다.**
- **⚠ KO CEO 가 2026-03-31 에 바뀌었다** — Quincey → **Henrique Braun**(내부 COO 승진).
  Quincey 는 Executive Chairman 으로 잔류, 회장·CEO 분리.
  **스터디의 재무 실적은 사실상 Quincey 체제의 결과다** — Braun 체제 온전한 분기가 아직 없다.
- **⚠ 검산 스크립트 6종 중 coca-cola 문서를 읽는 것은 하나도 없다.**
  `grep -rl coca docs/analysis/tools/*.js` → 없음.
  **편집이 안전하다는 뜻이자, 문서 드리프트를 아무도 못 잡는다는 뜻이다.**
  uber 처럼 파싱 앵커에 물려 있지 않으니 산문을 고쳐도 되지만, 수치를 고치면 잡아줄 게 없다.
- **EDGAR 재취득 — KO CIK `0000021344`**
  10-K FY2025 `0001628280-26-010047` (`ko-20251231.htm`) ·
  10-Q 2Q26 `0001628280-26-050503` · 10-Q 1Q26 `0001628280-26-028802` ·
  DEF 14A 2026 `0001104659-26-028215` ·
  **PepsiCo CIK `0000077476`** 10-K FY2025 `0000077476-26-000007` ·
  **KDP CIK `0001418135`** 10-K FY2025 `0001418135-26-000016`.
  User-Agent 에 연락처 이메일 필수.

**승계 — 전부 유효:**

- **⚠ `verify-uber-rim.js` 는 「문서 전체에서 첫 번째 매칭」에 의존한다.**
  `uber-스터디.md` 를 훑어 **처음 걸리는 값**을 쓴다(현재 CH.06 자사주 절의 `FY2025 $6.52B`,
  스크립트 330행). 문단 순서를 바꾸다 그 앞에 같은 패턴이 생기면 **조용히 다른 값을 집는다.**
- **⚠ `verify-uber-rim.js` 는 `uber-분석.md` 를 통째로 펴서 리터럴 공백으로 문장을 찾는다.**
  `const flat = anal.replace(/\n/g, ' ')` — 줄바꿈이 **공백 1개**가 되므로 두 줄에 걸친 문장이
  매칭된다. **빈 줄을 넣거나 줄을 합치면 매칭이 깨진다.** 이런 정규식이 25개다.
  고치려면 load-bearing 줄을 먼저 뽑아라(직전엔 37줄):
  `grep -n 'anal\.match\|flat\.match' docs/analysis/tools/verify-uber-rim.js`
- **⚠ 반대로 tsmc 분석 문서는 산문 편집이 안전하다** — `verify-tsmc-anchor` 는 ANCHOR 표·
  시나리오 표·금지패턴만 보고, `verify-tsmc-depreciation` 은 **파일을 아예 안 읽는다.**
- **⚠ 표 안에 빈 줄을 넣지 말 것** — 시나리오·IRR 표 정규식이 `\|…\|\n` 연속 행을 요구한다.
- **⚠ PASS/FAIL 만 보면 안 된다 — 출력 전문을 대조할 것.** 값이 바뀌어도 통과한다(`note()` 는
  판정하지 않는다). 편집 **전에** 기준선을 뜨고 나중에 `diff`:
  ```
  for s in verify-uber-rim verify-uber-anchor verify-tsmc-anchor verify-tsmc-depreciation; do
    node docs/analysis/tools/$s.js > /tmp/base-$s.txt 2>&1; done
  ```
  줄 번호만 달라지면 정규화해 비교하고, **검사 건수가 같은지도 볼 것.**
- **⚠ 어느 스크립트가 어느 파일을 읽는지**: `verify-uber-rim` 만 `uber-스터디.md` 를 읽는다.
  나머지는 `-분석.md` 와 posts 만. **스터디 편집은 uber 만 위험, 분석 편집은 셋 다 위험.**
- **`verify-lossless-edit.js` 사용법** — 고쳐 쓴 뒤 사실이 빠졌는지 본다.
  `node docs/analysis/tools/verify-lossless-edit.js companies/uber/uber-스터디.md [커밋]`
  숫자 토큰·영문 인용·출처 태그·제목·백틱 참조를 다중집합 비교. **새로 생긴 숫자도 FAIL** 이다.
- **⚠ 폰 실물 확인이 아직도 안 됐다(네 세션째).** 켈리 «논지·적정가» 화면은 아무도 안 봤다.
- **⚠ 데모 픽스처는 폰에서 못 본다** — `_selfcheck.html` 은 `.gitignore:1`.
- **⚠ DOM 스텁을 짤 때 `input.value` 는 문자열로 강제할 것.**
- **⚠ 앵커 검산은 기대값을 하드코딩하지 않는다** — 문서를 파싱한다. **표의 값 칸엔 숫자만.**
- **⚠ 재계산이 안 맞으면 먼저 10-K 원문을 열 것.** Uber 브리지 4,191 은 오류가 아니라 **서술
  누락**이었다. 세 에이전트가 만장일치로 «산술 오류»라 확정했던 건이다.
- **⚠ Netflix — 1H26 을 연환산하면 크게 틀린다.** WBD 인수 파기 위약금 **$2.8B** 이 1Q26 에
  일회성으로 들어가 있다. FY2025 10-K 만 읽으면 안 보인다.
- **⚠ Netflix — 2025-11-14 10-for-1 액면분할.** 위임장은 주당 금액을 전부 소급조정한다.
- **⚠ Netflix — 회원수·ARM 분기 공시가 2025년 1분기부터 폐지됐다.** 잔여 45건은 조사 부실이
  아니라 **공시 부재**다.
- **⚠ Netflix 는 자사주를 장부가의 16.8배에 산다** — ROE 42.8% 의 분모가 구조적으로 눌려 있다.
  공시 ROE 를 그대로 RIM 에 넣으면 안 된다(`netflix-분석.md` APP.A).
- **⚠ Netflix 판정 「점검」의 근거는 «부호»지 «숫자»다.** 세 시나리오 적정가가 전부 현재가
  $80.14 아래라는 것이 근거다. **«적정가 $24.60» 을 정본처럼 인용하면 틀린다.**
- **⚠ Netflix 내재 지속기간이 경로에 따라 83년 ↔ 15년으로 뒤집힌다.** 반사실 경로(b=1)는
  **진단용이지 적정가 산출용이 아니다.**
- **⚠ 세션이 커밋 없이 끝나면 다음 세션은 그 사실을 모른다. `git log` 가 아니라 `git status` 를
  먼저 볼 것. 부모와 분석 저장소 둘 다.**
- **⚠ `-25308` 키체인 경고**는 푸시 중 나올 수 있으나 무해하다. **이번 푸시에서 실제로 떴고,
  refspec 갱신 줄(`b9abb67..68f02f6`)과 동기화 상태로 성공을 확인했다.**
- **주가 소스는 여전히 미해결(Uber 미결 ⑩)** — 1차 시세는 사용자 몫.
- Fundsmith 13F CIK `0001569205`(다음 13F 2026년 11월 중순) · Netflix CIK `1065280` ·
  Uber CIK `1543151` · TSMC CIK `0001046179`.
- 도구 줄 번호 표 · 20-F vs 6-K 자본 1.2% 차 · ADS=보통주 5주 — `fdfea5a` 판 참조.

## 손대지 말 것

**이번 세션 신규:**

- **⛔ `coca-cola-스터디.md` 에 시장 점유율을 써넣지 말 것.**
  **KO·PEP·KDP 세 회사 10-K 어디에도 없어서 의도적으로 비웠다.**
  채우려면 유료 조사자료(Euromonitor·Nielsen)가 필요하고 그건 **추정** 등급이다.
  기억으로 채우는 순간 이 장 전체의 신뢰도가 무너진다(P1).
- **⛔ 「돈을 포기한 증거」를 TSMC 급으로 격상하지 말 것.**
  KO 에서 찾은 둘(원액 가격결정권 자발적 미행사 · 13세 미만 광고 금지)은 **금액이 미공시**고,
  incidence pricing 은 회사 스스로 "as a practical matter 경쟁 환경에 제약된다"고 적었다
  (`10K:190`). **애초에 못 쓸 권리였다면 포기가 아니다** — 그 경고를 본문에 달아 뒀다.
- **⛔ 피셔 F08(임원 간 관계)의 빈칸을 채우지 말 것.**
  공시로 답해지는 항목이 아니다. 스커틀벗이 필요하고 이번엔 안 했다.
  **비워 둔 것이 정상 동작이다** — tsmc 미결 ⑩ 과 같은 규율.
- **⛔ 「코카콜라 상표 장부가 0」을 공시 사실로 격상하지 말 것.**
  **구성**이다. 근거는 광고비 즉시 비용화(`10K:1317`)와 무형자산 분류 정책(`10K:1381`),
  그리고 KO 가 그 상표를 인수한 적이 없다는 사실뿐이다. 10-K 는 상표를 브랜드별로 안 쪼갠다.
- **⛔ 매출 10% 병입사를 CCEP 로 확정하지 말 것.** 회사는 이름을 안 밝혔고 단서는
  "EMEA 와 Asia Pacific 두 세그먼트"뿐이다(`10K:2684`). **지목은 구성이다.**
- **⛔ CH.04 부외자산 두 건을 겹쳐 세지 말 것.**
  「코카콜라 브랜드가 장부에 없다」(CH.02)와 「지분법 시가 초과분 $20,287M」(CH.04)은
  **별개다.** 합산하면 이중계상이다.
- **⛔ `coca-cola-스터디.md` 에 판단을 쓰지 말 것.** 싸다·비싸다·좋다는 `coca-cola-분석.md` 로
  간다. 이사회가 성분 리스크 주주제안에 "unnecessary and duplicative" 로 답한 건도
  **사실만 기록**하고 정직성 판정은 분석 문서로 넘겼다.

**승계 — 전부 유효:**

- **⛔ 미확인 잔여를 「조사 부족」으로 보고 억지로 메우지 말 것.** 대부분 회사가 공시를 접어서
  생긴 구멍이다. 못 채운 칸을 채운 척한 문서가 가장 위험하다.
- **⛔ netflix 스터디에 남은 200자 초과 3줄을 «정리»하지 말 것** — 전부 영문 원문 인용이다.
- **⛔ `uber-스터디.md` CH.06 자사주 절의 문장 순서를 바꾸지 말 것** — `FY2025 $6.52B` 가 앵커다.
- **⛔ `uber-분석.md` 의 문장을 다시 쓰지 말 것 — 빈 줄 넣기까지가 안전선이다**(`flat` 함정).
- **⛔ `netflix-분석.md` 의 앵커를 웹 집계치로 «갱신»하지 말 것.** $80.14 는 사용자 1차 입력이다.
- **⛔ 미결 번호를 재사용하거나 재번호하지 말 것**(netflix·uber·tsmc 전부. 본문이 상호참조한다).
- **⛔ Netflix 반사실 경로(경로 B)를 적정가 산출에 쓰지 말 것** — 진단용이다.
- **⛔ 회원수·ARM 공시 중단을 F15 탈락 사유로 쓰지 말 것.** 부정직이 아니라 공시 축소이고,
  요소 2 에서 이미 감점했다. **같은 사실로 두 번 벌주지 않는다.**
- **⛔ 분석 문서의 «덩어리 가르기»를 되돌리지 말 것.**
- **⛔ `verify-lossless-edit.js` 의 «새 숫자도 FAIL» 규칙을 완화하지 말 것.**
- **⛔ 검산 스크립트의 기대값을 문서에 맞추지 말 것** — FAIL 은 문서를 고칠 신호다.
- **⛔ 부모 `.gitignore:3` 의 `docs/analysis/` 줄을 지우지 말 것** — 그 한 줄이 분석 자료
  전체를 공개 저장소에서 가린다.
- **⛔ 켈리 후보에 현재가를 저장하도록 «개선»하지 말 것**(PRD §7-D27 기각 대안 ③).
  `stripReviewFields()` 에 `thesis`·적정가 넣기 금지 · `pushFairLog` 를 «저장할 때마다
  적립»으로 바꾸기 금지 · `FAIR_STALE_DAYS`(100) 감으로 조정 금지 ·
  적정가 범위를 편입 시 승계 금지(승계는 논지뿐).
- **TSMC 기준선 = 2단계 유보 4.91**(2026-08-16 사용자 확정). 정률 5.70 으로 되돌리지 말 것.
- Uber 앵커 표 값 칸에 주석 금지 · 요소 11 «중»을 «강»으로 되돌리기 금지 ·
  `<!-- ANCHOR -->` 마커와 행 이름 · APP.G/APP.A 에 절대 PBR 금지 · `prob` 을 "승률"로
  되부르기 금지. 상세는 `8a948ee` 판.

## 미결 — 다음 세션이 이어받을 것

1. **⭐ `coca-cola-분석.md` 가 없다 — 가장 큰 공백.**
   스터디가 재료를 다 갖췄으니 TOOL 01·02 판정 → 밸류에이션 → 적정가 → 기각 조건 순서로
   쓰면 된다. **기각 조건 1순위 후보는 IRS 항소심**(이산적 사건이라 번역이 쉽다).
   ⚠ **적정가 산출 전에 CH.04 의 세 왜곡을 어떻게 처리할지 먼저 정해야 한다** —
   자사주 −$56,423M 이 분모를 누르고, 코카콜라 브랜드가 자산에 없고, 지분법 초과분
   $20,287M 도 없다. **앞의 둘은 ROE 를 올리고 셋째는 내린다.** Netflix APP.A 와 같은 문제다.
2. **coca-cola 스터디 미확인 잔여 15건** (문서 끝). **AI 수행 가능한 값싼 확장 둘**:
   ⑪ 실적 발표 콜 원문(8-K 첨부·IR) — 경영진 자기 서술과 Q&A 를 전혀 못 봤다 ·
   ⑫ 병입사 개별 공시(CCEP·FEMSA·CCHBC·CCBJH 전부 상장사).
   ⑧ fairlife 최초 인수가는 **FY2020 10-K** 를 열면 나온다.
   ⑬ IRS 판결·⑭ Braun 첫 온전 분기·⑮ 아프리카 매각 종결은 **시간이 지나야 채워진다.**
3. **⭐ 폰 실물 확인 3건 (켈리 «논지·적정가», 네 세션째 미결)** — ⑴ 칸 7개 모달이 폰
   폭(360px)에서 안 터지는지 ⑵ 카드 두 줄이 읽히는지 ⑶ 현재가 거리 계산이 도는지.
   **피드백 오면 그걸 먼저 처리.**
4. **⭐ Netflix 미결 6건 중 ②③④⑦ 이 사용자 몫** (`netflix-분석.md` 끝).
   **④ 스커틀벗**이 TOOL 01 을 «경계 지역»에 붙잡고 있다. **AI 가능**: ⑩ 외부 교차검증.
5. **Uber 미결 큐 15건** (`uber-분석.md` 끝). **사용자 몫**: ①②③④ · ⑤ **요소 5 락인 1 vs 0**
   (밴드를 뒤집는 유일한 칸) · ⑧ · ⑩ · ⑫. **AI 가능**: ⑨⑪⑯⑰⑱.
6. **TSMC 미결 큐** (`tsmc-분석.md` 끝): ① 가설 초안 · ⑤ 중앙값 4.81 지위 · ⑧ 스커틀벗 ·
   ⑩ F08 · ⑬ 자본 1.2% 차 · ⑦ 13F 관찰(11월).
   **그리고 tsmc 스터디에 CH.06 이 없다** — 프리미엄 평가의 구조적 부채다.
7. **⑲ 는 도구 쪽 백로그다** — `index_roe.html` 에 유보율 b 입력이 없어(b=1 고정) 문서의
   Mode 1~4 표를 도구 화면으로 재현할 수 없다. **PRD §9 백로그.**
8. 도구 쪽 승계: `index_premium.html:575` 문구 정정(연동 3파일) · `kelly-pf-*` 폐기 키 ·
   프리미엄 11요소 원저 서지 확정(PRD §9).

## 검증 상태

- **`node test_pipeline.js` — 167 pass** (도구 무변경, 직전 판과 동일).
- **검산 6종 전부 FAIL 0** — `verify-template-rules` · `verify-uber-rim` · `verify-uber-anchor` ·
  `verify-tsmc-anchor` · `verify-tsmc-depreciation` · `verify-netflix-rim`.
  **coca-cola 문서는 어느 스크립트도 읽지 않으므로 기준선 대조는 불필요했다**
  (`grep -rl coca docs/analysis/tools/*.js` → 없음).
- **coca-cola 인용 검증 — 고유 124건 · 참조 줄 167개 · 빈 줄/페이지번호 0개.**
  스크립트로 모든 `약칭:줄번호` 를 원자료에 되짚어 실제 내용 줄에 걸리는지 확인했다.
- **coca-cola 핵심 수치 71건을 인용 줄과 기계 대조 — 실패 0.**
  매출·마진·현금흐름·세그먼트·경쟁사·보수·지분 전부. 「그 줄에 그 값이 실재하는가」를 봤다.
- **계산값은 Node 로 검산** — GM 61.6% · OM 28.7% · ROE 40.7% · capex/매출 4.4% ·
  조정 FCF 커버 130%/128%/123% · PBNA 3.9% · IB Franchise 35.4% 등.
- **가독성 측정** — 200자↑ 산문줄 0 · 산문 최장 139자 · 연속 산문 최대 5줄.
- **안 함**: 브라우저·폰 실물 · KO 실적 발표 콜 · 병입사 개별 공시 · 스커틀벗 ·
  `coca-cola` 문서에 대한 무손실 대조(신규 파일이라 비교 대상이 없다).

## 되돌리는 법

**분석 repo** (`docs/analysis` 안, 전부 푸시됨 — revert 도 푸시 필요):

- **`git revert 68f02f6`** — coca-cola 문서 셋 통째 제거. **신규 폴더라 부작용이 없다** —
  다른 무엇도 참조하지 않고, 검산 스크립트 어느 것도 이 파일을 읽지 않는다.
- `git revert b9abb67` — Netflix 분석 v1.0 만 원복, v0.1 자격 판정은 남는다
- `git revert 8d086ae` — `netflix-분석.md` 통째 제거 + 히스토리 이력 행 원복
- `git revert 0c8f2a6` / `048d2c3` — uber / tsmc 분석 덩어리 가르기만 원복
- `git revert 4758b07` / `fbed047` / `fe6ceb0` — tsmc / uber / netflix 스터디 가독성만 원복
- `git revert d30c71b` — `verify-lossless-edit.js` 제거. 다른 무엇도 참조하지 않는다.

문서별로 나눠 커밋했으므로 **어느 하나만 되돌려도 나머지는 그대로다.**
그 이전(`6210ff3` · `f1dd313` · `ec311ce` · `a4e4b62` · `11c759c`)의 안내는 `fdfea5a` 판 그대로.

**부모 저장소**: 이번 세션 커밋은 이 HANDOFF 갱신뿐이다.
켈리 기능 원복은 `git revert a94ebde 4768d6b eb2b810` — **데이터 안전하다**(필드가 전부 선택적
추가라 §7-D21 의 «의미 변경»에 해당하지 않는다). **`4768d6b` 단독 revert 는 금지** — 결정
기록만 사라지고 코드가 남아 왜 현재가를 안 저장하는지 아무도 모르게 된다.
