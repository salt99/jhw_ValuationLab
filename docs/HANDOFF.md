# HANDOFF

> 세션을 끝낼 때 이 파일을 **덮어쓴다.** 다음 세션은 이것부터 읽는다.
> 파일은 항상 하나다 — 지난 내용은 `git log docs/HANDOFF.md` 에 남는다.

**갱신**: 2026-08-16 · **브랜치**: `main` · **부모 `baf89d3`+이 커밋** · **분석 `777fe61` (푸시됨)**

## 지금 상태

**TSMC 분석이 v3.0 으로 완성된 세션(들)이었다. 부모 저장소는 이 HANDOFF 커밋뿐이다.**
켈리 도구는 `d523106` 그대로 배포돼 있다 — 도구 코드는 한 줄도 안 건드렸다.
작업은 전부 분석 repo 이고 **전부 푸시됐다**(`ea2ceb3`…`777fe61`, 5커밋).

한 일 여섯 가지:

1. **TSMC 재작성 (v2.0)** — `_template` 뼈대로 옮기며 TOOL 01·02 를 실제로 채웠다.
   판정: **능력범위 «경계 지역»(5/6) · 프리미엄 «자격 높음»(19/22=86%) · 밸류에이션
   «고평가» · 종합 «점검»**. 인버전 사전 부검 수행(거부권 해제), 20-F 4건 원문으로
   관문 4 채움, 상각 조정 ROE 해소(`APP.G` 신설)
2. **가격 앵커 정정 (v3.0)** — v2.0 의 "PBR 10.5" 는 2025년 연말 값을 현재가로 물려
   쓴 것이었다. **현재 PBR 11.01**(주가 8/14 종가 $426.35 ÷ 2026-06-30 자본 BPS $38.74)
   로 확정, 파생값 전부 재계산. **적정가(5.70)·기각 조건은 불변**
3. **ROE 도출 재구성** — 사용자 지적으로 10년 단순 평균(27.9%)을 폐기하고 **현재 기반
   브리지**(35.4% → 사이클 중립 → 전방 부담 = 29~30%)로 교체. 성장 정합 갈림길까지
   반영한 기준선 밴드 4.4~6.8 · 중심 ≈5.6 — **대표값 5.70 은 밴드 중심 부근이라 유지**
4. **시나리오 내러티브 신설** — BEAR/BASE/BULL 각각이 어떤 세계이고, 무엇이 순서대로
   일어나며, 무엇으로 그 세계에 있음을 아는지. 기각 조건 번호와 직접 묶었다
5. **디렉터리 재구성 + posts/** — `companies/` `masters/` `posts/` 버킷.
   **이전 HANDOFF 의 `docs/analysis/tsmc/` 류 경로는 전부 무효다**
6. **공개용 분석 글** — `posts/2026-08-15-tsmc.md` (ultracode 워크플로 12에이전트,
   내부 흔적·개인정보·원문 인용 배제 검증 완료)

### ⚠ 직전 HANDOFF 의 오류 두 건 — 이번에 코드 대조로 잡았다

이전 판(그리고 템플릿 사본)이 도구 규칙을 틀리게 적고 있었다. **둘 다 수정 완료**
(`_template/README.md`·`분석.md`, 분석 repo `184159f`). 다음 세션은 아래가 맞다:

1. **«범위 안» 은 6/6 뿐이다.** `Math.round(5/6×100)=83 < 84` 라 5/6 은 «경계 지역»
   (`index_circle.html:186,198`). "≥84% (6/6 또는 5/6)" 는 틀린 서술이었다
2. **`훌륭한 경영진` 은 min() 이 아니다.** 코드는 평균으로 등급을 내고(≥1.5 강,
   ≥0.75 중) **약(0) 이 있으면 강으로 못 올라가게 막을 뿐**이다
   (`index_premium.html:640-648`). "여섯이 강하고 하나가 약이면 결과는 약" 은
   틀렸다 — 결과는 **«중»** 이다. 도구 화면 문구(`:575`)가 "최약 고리가 상한"이라
   생긴 오해다. **화면 문구를 고칠지는 미결** — 라이브 문구라 임의로 안 바꿨다

## 분석 repo 구조 (재구성 후)

```
docs/analysis/                          ← private repo, 부모 .gitignore:3 이 가림
├── _template/          루트 유지
├── tools/              루트 유지 — verify-template-rules.js(82검사)
│                         · verify-tsmc-depreciation.js · verify-tsmc-anchor.js
├── companies/tsmc/     tsmc-분석.md(v3.0) · tsmc-스터디.md(v1.1) · tsmc-히스토리.md
├── masters/terry-smith/
└── posts/              2026-08-15-tsmc.md + README.md(내보내기 규칙)
```

## 알아야 할 것

- **⚠ 가격 앵커 규칙 — 주가와 자기자본은 같은 시점이어야 한다.** PBR 은 P₀/B₀ 다.
  한쪽만 갱신하면 **에러 없이 배수만 틀린다.** 이번에 실제로 두 번 틀렸다(연말 값
  8개월 물려 씀 / 최신 주가 ÷ 낡은 자본 = 12.96). `tsmc-분석.md` 의 「가격 앵커」
  표(`<!-- ANCHOR -->` 마커)가 정본이고, `tools/verify-tsmc-anchor.js` 가 산술·
  **시점 간격(100일 초과 FAIL)**·공개 글 동기화를 검사한다. **분기 갱신은 앵커 표
  6줄만 고치면 된다.** 고의 파손 시험으로 검사가 실제로 잡는 것을 확인했다

- **⚠ ADS 와 보통주를 섞으면 TSMC 는 5배가 어긋난다** (1 ADS = 보통주 5주,
  ADS 5.1865B). 시총 $1.98T 표기가 그 사고였다. 데이터 사이트끼리도 다르다 —
  stockanalysis.com 은 보통주 수에 낡은 주가를 곱해 틀린 시총을 낸다

- **⚠ tools/ 와 _template/ 를 버킷 안으로 옮기지 말 것.**
  `verify-template-rules.js:3` 이 부모 저장소를 `__dirname/../../..` 로 찾는다 —
  한 단계 깊어지면 **에러 없이 엉뚱한 디렉터리를 대조하며 전 검사가 통과한다.**
  이유는 분석 repo README 두 곳에 적어 뒀다

- **⚠ posts/ 도 private 다.** "게시물"은 형식이지 공개 상태가 아니다. 밖으로 낼 때
  지울 것(포트폴리오 맥락·매수 계획·제3자 원문)은 `posts/README.md` 에 있다.
  공개 글은 분석 문서를 가리키고 역방향 참조는 없다 — 판정이 바뀌면 글이 낡는다

- **⚠ 스커틀벗(관문 5)은 AI 가 대신할 수 없고, 그 한 칸이 TOOL 01 을 «경계 지역» 에
  붙잡고 있다.** 그게 정상 동작이다. 검색으로 읽은 것은 관문 4(1차 자료) 쪽이다.
  **판정이 나쁘다고 기준을 낮추지 말 것** — TSMC 미결 ⑧

- **켈리 탐색 결과 (도구 코드로 실측)** — TSMC 를 지금 넣으면 `kellyOf` 가
  `'상단 목표가조차 현재가 이하 — 매수 구간이 아닙니다'` 로 보류시킨다. 세 목표가
  (BULL 8.04 포함)가 전부 현재가 아래라 **확률을 뭘로 넣든 같다.** f 가 양수로 도는
  것은 PBR 5.7 언저리부터, 5.0 아래면 대부분 상한 30% 에 걸린다. **넣게 되면 $ 환산은
  `목표가 = 현재가 × (목표PBR/11.01)`** — BPS 를 곱하면 안 된다(문서 PBR 기준과
  시세가 어긋난다). 3중 집중 심사(인적자본+섹터+TSMC)가 선행 조건

- **EDGAR 재취득법** — User-Agent 에 **연락처 이메일** 필수. TSMC CIK `0001046179`,
  FY2025 20-F acc `0001628280-26-025362`, 2Q26 6-K acc `0001046179-26-000541`.
  Fundsmith 13F 는 CIK `0001569205`, 다음 13F 는 **2026년 11월 중순**

- **20-F 와 6-K 의 자기자본이 1.2% 다르다**(2025-12-31 기준 5,355,039 vs 5,419,596
  NT$M) — 원인 미확인(TSMC 미결 ⑬). 앵커는 6-K, `APP.G` 는 20-F 로 각각 일관 사용

- **Mode 4 IRR 엔진의 종료 가정이 미확정이다**(TSMC 미결 ⑪) — 재구성 엔진이
  항등식(적정가→10.0%)은 만족하나 v2.0 의 두 값(5.0%·13.1%)을 재현 못 한다
  (5.6%·12.6%). 항등식은 재구성이 원본과 같다는 증거가 아니다. 결론엔 영향 없음

- **줄 번호는 `d523106` 기준 그대로다** — 이번에도 도구 코드 무변경. 인용 전
  `grep -n` 확인 습관 유지 (stale 참조 사고 2회 이력: `f233930`, `d459558`)

  | 대상 | 현재 줄 |
  |---|---|
  | `scenLine()` / 호출 2곳 | `index_kelly.html:1047` / `:1365`, `:1499` |
  | "상방 없음" 가드 / `compute()` | `:945` / `:992` |
  | `MAX_SINGLE` / `THIN_DOWNSIDE` | `:929` / `:928` |
  | `kellyOf()` / `kelly3()` | `:931` / `:800` |
  | `colorOf` 맵 / `thesisWorsened` 가드 | `:1314` / `:1432` |
  | 6관문 판정 로직 / veto | `index_circle.html:198` / `:188` |
  | `mgmtDerived()` / `scoreData()` / 밴드 60/35 | `index_premium.html:640` / `:867` / `:876` |

- **켈리 v1 유산·설계 문서 어긋남·엄밀한 제약 하 켈리 아님 문단** — 직전 HANDOFF
  내용 그대로 유효하다. 필요하면 `git log docs/HANDOFF.md` 에서 `baf89d3` 판을 볼 것.
  요지: `docs/superpowers/specs/2026-08-13-*.md` 의 MAX_SINGLE 40%·하프 켈리 서술은
  D22·D23 으로 대체됨(정본은 PRD §4.4 와 코드) · 상한 초과분은 채권으로(이유 있음) ·
  PBR 입력 시 f 는 맞고 주수가 틀림

- **`-25308` 키체인 경고는 간헐적이다** — 이번 세션 두 번의 푸시 중 한 번은 안 떴고
  한 번은 떴다. 어느 쪽이든 **푸시는 정상 완료**된다. 실패로 오해하지 말 것

## 손대지 말 것

- **⚠ `.gitignore:3` 의 `docs/analysis/` 줄** — 지우면 private repo 의 `.git` 째로
  공개 저장소에 섞인다. 복구 불가
- **분석 자료를 부모 저장소에 커밋하지 말 것** — 갈 곳은 `jhw_ValuationLab_analysis`
- **「가격 앵커」 표의 `<!-- ANCHOR -->` 마커와 행 이름** — `verify-tsmc-anchor.js`
  가 이 문자열로 파싱한다. 표 형식을 바꾸면 검사가 표를 못 찾고 죽는다(그건 FAIL 로
  드러나니 차라리 낫다). **값 갱신은 6줄만**
- **`APP.G` 에 절대 PBR 을 다시 적지 말 것** — 그 절의 자본은 FY2025 기말이라 시세와
  나누면 시점이 어긋난다. 비율 진술만. 검사기의 금지 패턴 검사가 지킨다
- **부록 글자 재사용 금지** — 구 `APP.B·D·E·F` 는 본문으로 흡수돼 비었다. 신설 절은
  `APP.G` 다음 글자부터. 재사용하면 옛 참조가 조용히 다른 곳을 가리킨다
- **`_template/` 의 판정 규칙 사본을 "정리"하지 말 것** — 고칠 일이 있으면 도구
  코드를 먼저 고치고 `verify-template-rules.js` 로 맞춘다. 이번 최약 고리 수정도
  검사기가 잡은 방식(도구 문구 앵커 유지 + 실제 계산 병기)으로만 통과된다
- **도구 관련**: `kellyOf` 의 순서 검증·선언 형태(`function kellyOf(h){` — 테스트가
  중괄호 균형으로 찾음)·`kelly3` 의 α≈0∧β≈0 가드·`colorOf` 맵·`thesisWorsened` 의
  `prevPBase` 가드·마이그레이션에 기본값 채우지 않기·`compute()` 의
  `rel=w/equityScale`·`--demo` 픽스처 — 전부 직전 HANDOFF 그대로 유효
- **`prob` 을 화면에서 "승률"로 되부르지 말 것** — v5 의 prob = P(Bull). 되돌리면
  예외 없이 배분만 틀린다

## 미결 — 다음 세션이 이어받을 것

### 답을 기다리는 것

0-A. **⚠ 공개 히스토리의 근무처 노출** — `1ec0e72`·`a2ed35d` 에 남아 푸시된 상태.
   ⓐ 현재 파일만 정리(됨) ⓑ `git filter-repo` + force-push ⓒ 무시.
   **두 세션째 답이 없다. ⓑ 는 사용자 결정 없이 실행 금지**
0-B. **공개 글 파일명 날짜** — `2026-08-15-tsmc.md` 인데 내용 기준일은 8/14 종가다.
   그대로 둘지 물었고 답 없음
0-C. **`index_premium.html:575` 화면 문구** — "최약 고리(weakest link)가 상한을
   정한다"가 실제 계산(평균+약 시 강 불가)과 다르다. 고치면
   `verify-template-rules.js` 의 문구 앵커 검사도 같이 움직여야 한다

### TSMC 문서 미결 큐 (`tsmc-분석.md` 끝, ①~⑬)

핵심만: ⑧ 스커틀벗(사용자 몫 — «범위 안» 승급의 유일한 길) · ⑨ 인버전 경로 6
(정치의 자본배분 개입)의 기각 조건 번역 · ⑩ 피셔 F07·F08 1차 자료 · ⑪ IRR 엔진
종료 가정 · ⑫ 현재가 갱신 절차(앵커 검사기가 절반은 해결 — 남은 건 분기 점검
절차에 시세 재취득 단계 명문화) · ⑬ 20-F vs 6-K 자본 차이 · ⑦ 13F 2026Q3 관찰
(11월 중순, 기각 조건 아님)

### 도구 쪽 (변동 없음, 직전 HANDOFF 승계)

- 폰 실기 확인 누적 3건(사이징·3-시나리오 표현·`scenLine` 360px) · 각 보유 종목
  Base 채우기 · `kelly-pf-*` 폐기 키 3안 미선택 · 재평가 줄에 Base 표시 여부 ·
  허브 h2 명칭 · 목표 종목 수 5/방어선 3(다음 종목은 반도체 밖 권장, §P8)
- 리서치(P1): 피셔 분산 원문 숫자 미확인 · 프리미엄 11요소 원저 서지

## 검증 상태

- **분석 repo 검사 3종 전부 통과** — `verify-template-rules.js`(불일치 0) ·
  `verify-tsmc-depreciation.js`(빈티지 모델이 공시를 0.1~0.5% 오차로 재현) ·
  `verify-tsmc-anchor.js`(산술·시점 45일·공개 글 동기화, **고의 파손 2방향 시험 포함**)
- **공개 글 독립 검증** — 내부 흔적·개인정보·원문 전재 0건, 신규 파생 계산 6건 검산
- **구 문서 무손실 확인** — v1 대비 토큰 대조로 실질 누락 8건 복원
- 안 함: `node test_pipeline.js`(직전 기준 131 pass, 코드 무변경) · 자체 검사 · 폰 실기

자체 검사 돌리는 법(GUI 없는 세션)·`grep -c FAIL` 함정은 직전 판(`baf89d3`) 그대로.

## 되돌리는 법

분석 repo (`docs/analysis` 안에서, 전부 푸시됨 — revert 도 푸시 필요):

- `git revert 777fe61` — 앵커 검사기 제거
- `git revert 72ccffc` — 공개 글 제거
- `git revert 8589205` — v3.0 → v2.0 (앵커 12.96 오류 시절이 아니라 v2.0 커밋본으로)
- `git revert ab76ed5 ea2ceb3` — posts/ 와 버킷 구조 원복. **경로 참조가 연쇄되므로
  둘을 따로 되돌리지 말 것**
- TSMC 재작성 전 원문: `git show 73a6040:tsmc/tsmc-분석.md` (362줄 v1.0)

부모 저장소: 이 HANDOFF 커밋 하나. 도구 코드·`.gitignore` 무변경.
3-시나리오 이전으로의 도구 롤백은 **데이터가 안전하지 않다**(prob 의미 변화) —
절차는 직전 판 참조.
