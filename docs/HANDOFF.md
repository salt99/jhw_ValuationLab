# HANDOFF

> 세션을 끝낼 때 이 파일을 **덮어쓴다.** 다음 세션은 이것부터 읽는다.
> 파일은 항상 하나다 — 지난 내용은 `git log docs/HANDOFF.md` 에 남는다.

**갱신**: 2026-08-21 · **브랜치**: `main` · **부모 `eafc9de`+이 커밋** · **분석 `6210ff3` (푸시됨)**

## 지금 상태

**이번 세션은 도구 코드를 한 줄도 안 고쳤다.** 한 일은 하나다 — 배경 에이전트가 만들어 놓고
untracked 로 방치돼 있던 **Netflix 스터디를 분석 저장소에 커밋·푸시했다**(`6210ff3`).

부모 저장소는 `eafc9de` 그대로다. 켈리 «논지·적정가»(§7-D27)에 대한 직전 판 설명은 **전부
그대로 유효하다** — 설계 의도 셋, 낡음 배지 100일, 손대지 말 것 넷. 필요하면 `eafc9de` 판을 볼 것.

### Netflix — 재료만 깔렸다, 판단은 없다

`docs/analysis/companies/netflix/` 에 두 파일. **`netflix-분석.md` 는 미착수** — TSMC 때
파이프라인을 역행했던 교훈대로 Uber 와 같은 순서(재료 먼저)를 지켰다.

| 파일 | 크기 | 판 |
|---|---|---|
| `netflix-스터디.md` | 849줄 | **v1.0** · 2026-08-20 |
| `netflix-히스토리.md` | 21줄 | 이력 1행 |

본문 6장(`_template/스터디.md` 골격 그대로) + **미확인 잔여 45건**(Uber 13건).
1차 자료: FY2019~FY2025 10-K · 10-Q 6건 · 주주서한 8건 · DEF 14A 2026 · XBRL(CIK **1065280**).

**경위와 배운 것은 `netflix-히스토리.md` 이력 1행에 있다. 여기 옮겨 적지 않는다** — 두 벌로
두면 갈라진다. 다만 다음 세션이 **모르면 틀리는 것 셋**만 아래 「알아야 할 것」에 올렸다.

커밋 전에 확인한 것: `node tools/verify-template-rules.js` 불일치 0 · 목차가 `_template` ·
uber 스터디와 동일 골격 · 부모 `.gitignore:3` 이 여전히 `docs/analysis/` 를 가림
(`git check-ignore` 로 실물 확인 — 공개 저장소로 새지 않는다).

**날짜 불일치 1건을 고쳤다.** 히스토리 이력 행이 `2026-08-21`, 스터디 헤더·기준일이
`2026-08-20` 이었다(작성이 자정을 넘겨 갈라졌다). uber 는 둘이 같다 — **이력 행 날짜 = 문서
버전 날짜**로 맞췄다(사용자 판단). 미푸시 커밋에 amend 했으므로 별도 커밋이 없다.

## 알아야 할 것

**Netflix 신규 — 모르면 틀리는 것 셋:**

- **⚠ 1H26 을 연환산하면 크게 틀린다.** WBD 인수 파기(2026-02-27)로 받은 위약금 **$2.8B** 이
  1Q26 순이익·영업현금흐름에 **일회성으로** 들어가 있다. **FY2025 10-K 만 읽으면 안 보인다** —
  거기서는 인수가 «진행 중»으로 읽히고, 2Q26 10-Q 를 열어야 파기와 수령이 나온다.
  스터디는 CH.03·04·06 세 자리에 반영해 뒀다.
- **⚠ 2025-11-14 10-for-1 액면분할.** 분할 전 주당 수치와 섞으면 10배 오류.
  **위임장(DEF 14A)은 주당 금액을 전부 소급조정한다** — 위임장에서 역산한 값을 «그날 종가»라
  부르면 안 된다. 스터디가 스스로 경고해 놓고 집필 중 실제로 한 번 저질렀던 함정이다.
- **⚠ 회원수·ARM 분기 공시가 2025년 1분기부터 폐지됐다.** 시청시간도 2027년부터 연 1회로
  축소 예고. 이 회사는 **「부재의 관찰」이 핵심 재료**다. 잔여 45건이 Uber 13건의 세 배인 것은
  조사가 부실해서가 아니라 **공시가 없어서**다.
- Netflix 전용 검산 스크립트는 **없다**(uber/tsmc 와 다르다). 분석 문서를 쓸 때 만들지 여부는
  그때 판단할 것.

**승계 — 직전 판(`eafc9de`) 그대로 유효:**

- **⚠ 폰 실물 확인이 아직도 안 됐다.** 이번 세션도 GUI 가 없었다. 켈리 «논지·적정가» 화면은
  **여전히 아무도 안 봤다.** 로직만 Node 로 검증된 상태다.
- **⚠ 데모 픽스처는 폰에서 못 본다** — `_selfcheck.html` 은 `.gitignore:1`. 데스크톱에서
  `node docs/superpowers/build-selfcheck.js --demo && open _selfcheck.html`.
- **⚠ 낡음 배지는 폰에서 자연 재현이 안 된다** — 백업 JSON 의 `fairDate` 를 100일 이전으로
  고쳐 가져와야 보인다.
- **⚠ DOM 스텁을 짤 때 `input.value` 는 문자열로 강제할 것.** 안 하면 `.trim()` 이 터지는데
  코드 버그로 보인다. 스텁 버그다.
- **⚠ 검산 스크립트가 «문서를 파싱한다»** — `verify-uber-rim.js`(129건)·`verify-uber-anchor.js`
  (83건)는 기대값을 하드코딩하지 않는다. **표의 값 칸에는 숫자만 둘 것.**
  `cells()` 가 `*` 를 지우고 `−`(U+2212)를 `-` 로 바꾸므로 정규식은 정제 *후* 형태로.
- **⚠ 재계산이 안 맞으면 먼저 10-K 원문을 열 것.** Uber 브리지 4,191 은 오류가 아니라 **서술
  누락**이었다(지분법손실 53·비지배지분 40 차감). 세 에이전트가 만장일치로 «산술 오류»라
  확정했던 건이다.
- **⚠ 세션이 커밋 없이 끝나면 다음 세션은 그 사실을 모른다. `git log` 가 아니라 `git status` 를
  먼저 볼 것.** 이번 세션이 정확히 그 경우였다 — Netflix 두 파일이 untracked 로 떠 있었다.
  **부모와 분석 저장소 둘 다 볼 것.**
- **EDGAR 재취득법** — User-Agent 에 연락처 이메일 필수. **Uber CIK `1543151`** ·
  DEF 14A `0001308179-26-000125` · FY2025 10-K `0001543151-26-000015` ·
  2Q26 10-Q `0001543151-26-000032` · **Netflix CIK `1065280`** · TSMC CIK `0001046179` ·
  Fundsmith 13F CIK `0001569205`(다음 13F 2026년 11월 중순).
- **주가 소스는 여전히 미해결(Uber 미결 ⑩)** — 공개 시세가 봇 차단(JS 챌린지). $75.95 는 웹
  집계 2곳 교차치. 1차 시세는 사용자 몫.
- **켈리 도구에 Uber 를 넣는 것은 아직 이르다** — 미결 ⑫가 사용자 몫이고 세 시나리오
  적정가(0.92~3.05)가 전부 현재가 아래다. 후보로 등록해 논지·적정가만 적어 두는 것은 가능.
- **⚠ `-25308` 키체인 경고**는 푸시 중 나오지만 무해하다. 이번 푸시에서도 떴고, `git fetch`
  후 `origin/main` 을 되읽어 반영을 확인했다.
- 도구 줄 번호 표 · 20-F vs 6-K 자본 1.2% 차 · ADS=보통주 5주 — `fdfea5a` 판 참조.

## 손대지 말 것

**Netflix 신규:**

- **⛔ 미확인 잔여 45건을 「조사 부족」으로 보고 억지로 메우지 말 것.** 대부분은 회사가 공시를
  접어서 생긴 구멍이다. 못 채운 칸을 채운 척한 문서가 가장 위험하다.
- **⛔ `netflix-스터디.md` 에 판단을 쓰지 말 것.** 싸다·비싸다·좋다는 `netflix-분석.md` 로
  간다. 재료와 해석을 섞지 않는 것이 이 저장소 구조의 전부다(분석 repo `README.md`).
- **⛔ 히스토리 이력 행 날짜를 `2026-08-21` 로 되돌리지 말 것.** 스터디 헤더·기준일과 맞춘
  것이고 uber 도 같은 규칙이다.

**승계 — 전부 유효:**

- **⛔ 켈리 후보에 현재가를 저장하도록 «개선»하지 말 것** — §7-D27 기각 대안 ③. 설계다.
- **⛔ `stripReviewFields()` 에 `thesis`·적정가를 넣지 말 것.**
- **⛔ `pushFairLog` 를 «저장할 때마다 적립»으로 바꾸지 말 것.**
- **⛔ `FAIR_STALE_DAYS`(100)를 감으로 조정하지 말 것** — 분석 저장소 앵커 검사기와 같은 값.
- **⛔ 적정가 범위를 편입 시 `up`/`base`/`down` 으로 승계하지 말 것.** 승계하는 건 논지뿐.
- **⛔ 부모 `.gitignore:3` 의 `docs/analysis/` 줄을 지우지 말 것** — 그 한 줄이 분석 자료
  전체를 공개 저장소에서 가린다.
- `<!-- ANCHOR -->` 마커와 행 이름 · APP.G/APP.A 에 절대 PBR 금지 · 부록 글자 재사용 금지 ·
  `_template` 판정 규칙 사본 임의 정리 금지 · `prob` 을 "승률"로 되부르기 금지.
  상세는 `8a948ee` 판.
- **TSMC 기준선 = 2단계 유보 4.91** (2026-08-16 사용자 확정). 정률 5.70 으로 되돌리지 말 것.
  참고 상한(14.25)은 정률 유보 유지 — 물리 제약 위반을 보여주는 경계 표시다.
- **Uber 문서의 미결 번호를 재사용하지 말 것**(⑥⑦⑭ 자리는 비어 있다. 본문 18곳이 상호참조).
  앵커 표의 값 칸에 주석 금지 · 검산 스크립트의 기대값을 문서에 맞추지 말 것(FAIL 은 문서를
  고칠 신호) · 요소 11 의 «중»을 «강»으로 되돌리지 말 것(제3자 관찰 필요).

## 미결 — 다음 세션이 이어받을 것

1. **⭐ 폰 실물 확인 3건 (켈리 «논지·적정가», 두 세션째 미결)** — ⑴ 칸 7개 모달이 폰
   폭(360px)에서 안 터지는지 ⑵ 카드 두 줄이 읽히는지(논지가 길 때 줄바꿈) ⑶ 현재가 실시간
   거리 계산이 도는지. **피드백 오면 그걸 먼저 처리.**
2. **⭐ `netflix-분석.md` 착수** — 스터디가 깔렸으니 판단 문서가 다음이다. Uber 는
   스터디(`e580b54`) → 이틀 뒤 분석(`ec311ce`) 순서였다. 착수 전 `_template/분석.md` 와
   `verify-template-rules.js` 의 절 순서 규칙을 볼 것.
3. **Uber 미결 큐 15건** (`uber-분석.md` 끝). **사용자 몫**: ① 스커틀벗 · ② 2분 드릴 ·
   ③ 10년 확신 조건부 수용 · ④ 한 문장 가설 · ⑤ **요소 5 락인 1 vs 0**(밴드를 뒤집는 유일한
   칸) · ⑧ 임계 확인 · ⑩ 주가 1차 출처 · ⑫ 사이징 선행 심사.
   **AI 수행 가능**: ⑨ EU 지침 추적 소스 · ⑪ IR 게시일 · ⑯ F15 영구 장치 질문 ·
   ⑰ 브리지 run-rate 분자 원문 · ⑱ clean surplus 잔차 +164 계정 특정.
4. **⑲ 는 도구 쪽 백로그다** — `index_roe.html` 에 유보율 b 입력이 없어(b=1 고정) 문서의
   Mode 1~4 표를 도구 화면으로 재현할 수 없다. **PRD §9 백로그.**
5. **TSMC 미결 큐** (`tsmc-분석.md` 끝): ① 반증 가능 가설 초안 채택(사용자 몫) ·
   ⑤ 중앙값 4.81 지위 재평가 · ⑧ 스커틀벗 · ⑩ F08 · ⑬ 자본 1.2% 차 · ⑦ 13F 관찰(11월).
6. 도구 쪽 승계: `index_premium.html:575` 문구 정정(연동 3파일) · `kelly-pf-*` 폐기 키 ·
   프리미엄 11요소 원저 서지 확정(PRD §9).

## 검증 상태

- **`node test_pipeline.js` — 167 pass** (이번 세션 재실행, 직전 판과 동일. 도구 무변경이므로
  당연한 결과이나 회귀가 없음을 확인했다).
- **`node docs/analysis/tools/verify-template-rules.js` — 불일치 0** (Netflix 커밋 전 실행).
- **Netflix 스터디 구조 확인** — 목차가 `_template/스터디.md`·`uber-스터디.md` 와 동일 골격.
  잔여 45건은 800~844행(46번째로 잡히는 것은 헤더 18행의 장 목록이다).
- **푸시 반영 확인** — `git fetch` 후 `origin/main` 에서 파일 2개와 수정한 날짜 칸을 되읽었다.
- **안 함**: 브라우저·폰 실물 · Netflix 수치의 EDGAR 원문 재대조(스터디 집필 단계에서 이미
  수행됐고 이번 세션은 커밋만 했다) · Uber/TSMC 검산 스크립트 재실행(문서 무변경).

## 되돌리는 법

**분석 repo** (`docs/analysis` 안, 푸시됨 — revert 도 푸시 필요):

- `git revert 6210ff3` — Netflix 스터디·히스토리 통째 제거. **부모 저장소·도구에 영향 없다**
  (파일 추가뿐이고 부모는 `.gitignore:3` 으로 이 디렉터리를 아예 안 본다).
- 그 이전(`f1dd313` · `ec311ce` · `a4e4b62` · `11c759c`)의 revert 안내는 `fdfea5a` 판 그대로.

**부모 저장소**: 이번 세션 커밋은 이 HANDOFF 갱신뿐이다.
켈리 기능 원복은 `git revert a94ebde 4768d6b eb2b810` — **데이터 안전하다**(필드가 전부 선택적
추가라 §7-D21 의 «의미 변경»에 해당하지 않는다). **`4768d6b` 단독 revert 는 금지** — 결정
기록만 사라지고 코드가 남아 왜 현재가를 안 저장하는지 아무도 모르게 된다.
