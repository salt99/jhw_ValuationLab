# jhw_ValuationLab — PRD

> **문서 성격**: 역설계(reverse-engineered) 제품 정의. 배포 코드(2026-07-10 기준 main 브랜치)와 개발 과정의 의사결정 기록에서 추출.
> **목적**: 사람이든 AI든, 이후 작업 세션이 **이 문서 하나로 설계 의도를 이어받는 것**. 기능 카탈로그가 아니라 원칙·결정·제약의 전달이 우선.
> **유지 규칙**: 도구를 추가하거나 크게 개편할 때 §4(명세)·§5(키 레지스트리)·§7(결정 기록)을 갱신한다.

---

## 1. 제품 정의

**한 줄**: 가치투자 의사결정 파이프라인 — *"판단할 자격이 있는가 → 프리미엄 받을 사업인가 → 숫자로 얼마인가 → 얼마나 살 것인가"* — 를 4개의 독립 웹 도구로 구현한 개인용 분석 스위트.

| 항목 | 내용 |
|---|---|
| 사용자 | 개인 투자자 1인 (집중·장기 투자 실무, Fisher-style) |
| 이론 기반 | Fisher 『Common Stocks and Uncommon Profits』(거인의 어깨 2), Graham, Munger, Lynch, Popper/Soros, Kelly criterion |
| 배포 | GitHub Pages (`salt99.github.io/jhw_ValuationLab/`), 서버리스 |
| 형태 | 단일 파일 HTML × 4 + 허브, vanilla JS, 모바일 우선(홈 화면 웹앱) |
| 데이터 | 전부 브라우저 `localStorage` — 서버 전송 없음, 기기별 분리 |

**의도된 사용 흐름**: TOOL 01 → 02 → 03 → 04 순서. 앞 단계를 통과한 종목만 다음 단계로. (허브 하단에 명시)

---

## 2. 제품 원칙

**P1. 출처 검증 원칙 (non-negotiable)** — 체크리스트 항목은 거장이 실제로 말한 것으로 검증된 것만 수록한다. 추론·응용으로 구성한 항목은 삭제하거나, 남길 경우 "구성"임을 명시 표기한다. *배경: 개발 중 팩트체크에서 오귀속 3건 발견(§7-D1). 이후 전 도구에 적용되는 최상위 원칙이 됨.*

**P2. 로컬 우선** — 서버·로그인·외부 API 없음. 모든 상태는 `localStorage`. 도구별 키 네임스페이스 분리 필수(§5). 백업은 export/import JSON으로 사용자가 직접.

**P3. 모바일 우선** — 폰 화면 기준 레이아웃, 다크 테마, 홈 화면 추가 전제(`apple-mobile-web-app-capable`). 단일 파일 HTML로 오프라인 캐시 친화.

**P4. 사고 보조 ≠ 매매 지시** — 모든 도구에 가정·한계 디스클레이머를 화면 안에 명시한다. 입력이 추정치면 출력도 추정치(GIGO).

**P5. 검증 후 배포** — 수식·역산 로직은 배포 전 Node.js(DOM-stub)로 단위 검증한다. 대화 중 손계산 값과 코드 출력의 일치를 확인.

**P6. 과공학 경계** — 실사용 흐름에서 필요가 확인된 기능만 추가. "있으면 좋은" 기능은 기각.

**P7. 플랫 파일 규칙** — 모든 도구는 저장소 루트의 `index_{name}.html`. 허브만 `index.html`(GitHub Pages 진입점). 폴더 구조 금지(§7-D2).

**P8. 칸 회계** — 신규 편입 전 첫 질문은 "좋은 회사인가"가 아니라 **"이미 뚫린 칸과 다른 칸인가"**다. 인적자본(반도체 커리어·성과급)은 되돌릴 수 없는 1칸으로 계상한다 — 금융자본보다 큰 칸이며 이직 외에는 줄일 수단이 없다. 기각 판단은 기록되어야 반증 가능하다: 기각 사유·당시 주가·재검토 트리거가 남지 않으면 3년 뒤 밸류에이션 가정(§4.3의 `r`·`N`)으로 피드백이 돌아가지 못한다. *배경: TOOL 04 후보 파이프라인(§4.4).*

---

## 3. 정보 구조

```
jhw_ValuationLab/  (GitHub Pages root)
├── index.html            허브 — 4개 도구 카드, 워크플로 안내
├── index_circle.html     TOOL 01 · 능력범위 체크리스트      [골드]
├── index_premium.html    TOOL 02 · 기업 프리미엄 체크리스트  [앰버]
├── index_roe.html        TOOL 03 · 밸류에이션 4-모드 분석기  [민트]
├── index_kelly.html      TOOL 04 · 켈리 · 분할매수 플래너    [블루]
├── README.md             퀵스타트 (⚠ §8 드리프트 참고)
└── PRD.md                이 문서
```

허브 카드는 도구별 액센트 색으로 구분. 새 도구 추가 시: `index_{name}.html` 생성 → 허브에 카드 추가(TOOL 0N 번호·색 부여) → §5에 키 등록.

---

## 4. 도구별 명세

### 4.1 TOOL 01 — 능력범위 체크리스트 (`index_circle.html`)

**핵심 질문**: "이 종목을 판단할 자격이 내게 있는가?"

**항목 (6개, 전부 출처 검증됨)**

| # | 항목 | 출처 |
|---|---|---|
| 1 | 전문용어 없이 2분 안에 사업 설명 | 피터 린치 — two-minute drill, 『One Up on Wall Street』 |
| 2 | 5·10·20년 후 이익이 높아질 합리적 확신 | 워런 버핏 — 1996년 주주서한 |
| 3 | 실패 경로를 역방향으로 구성·반박 (인버전) | 찰리 멍거 — "invert, always invert" (야코비 차용) |
| 4 | 사업보고서 원문(DART) 직접 정독 | 벤저민 그레이엄 — 『증권분석』 |
| 5 | 회사 밖 독립 채널 직접 확인 (스커틀벗) | 필립 피셔 — 『Common Stocks and Uncommon Profits』 |
| 6 | 반증 조건을 사전에 명시 (텍스트 입력 포함) | 칼 포퍼 반증주의 → 조지 소로스 |

**판정 로직**: 충족률 84%↑ = 범위 안 / 50~83% = 경계 / 50% 미만 = 범위 밖.
**Veto**: 항목 3(인버전) 미충족 시 점수와 무관하게 최대 "경계" — "범위 안" 등급 불가. *근거: 호감을 분석으로 착각하는 것을 단일 실패점으로 간주(§7-D4).*

**저장**: 종목별 진단 스냅샷 저장/불러오기/삭제. 키: `coc-index`(목록), `coc-record-{id}`(개별).

### 4.2 TOOL 02 — 기업 프리미엄 평가 체크리스트 (`index_premium.html`)

**핵심 질문**: "이 사업이 높은 멀티플을 받을 자격이 있는가, 그리고 시장은 지금 얼마를 주고 있는가?"

**구조**: 프리미엄 자격 11개 요소 평가(거인의 어깨 2 기반; 반복구매·락인·가격결정력 등) → **종합 판단: 자격 × 시장가격의 3×3 매트릭스** → 매수·관망·매도 신호 도출. 크라이시스 시그널(위기 국면 기회 플래그) 포함. 평가 이력 저장.

**저장**: 키 `premiumLab:v1:*`(평가), `premiumLab:hist:*`(이력), `premiumLab:lastTicker`.

*비고: 11개 요소의 서지 정보(판본)는 원저 확인 후 보완 예정 — 코드 내 주석에 동일 취지 기재됨.*

### 4.3 TOOL 03 — 밸류에이션 4-모드 분석기 (`index_roe.html`)

**핵심 아이디어**: PBR · 초과ROE · 지속기간 N · 요구수익률 r 이 잔여이익(Residual Income) 방정식 하나로 묶이므로, **3개를 알면 나머지 1개를 역산**할 수 있다.

**공식** (전액 유보 가정 — 자기자본이 ROE로 복리 성장):

```
적정 PBR* = 1 + Σ[(ROE − r)·(1+ROE)^(t−1)] / (1+r)^t   (t = 1..N)
장기수익률 = [(1+ROE)^N · (청산PBR/진입PBR)]^(1/N) − 1
```

**4개 모드**

| 모드 | 미지수 | 답하는 질문 | 해법 |
|---|---|---|---|
| 1 적정가 | PBR | 정당한 PBR은? | 직접 계산 |
| 2 역산 | ROE | 이 가격이 정당화될 ROE는? | 이분법 |
| 3 역산 | N | 시장이 가정하는 moat 수명은? | 보간 |
| 4 수익률 | r(연수익) | 이 가격에 사면 연 몇 %? | IRR형 (청산 PBR 입력 추가) |

**기본 가정**: 전액 유보(무배당), N년 후 초과수익 소멸(ROE→r), 청산 PBR 기본값 1.0(보수적, §7-D9). ROE=r이면 PBR=1(가치 무차별)이 모델의 앵커.

**저장**: 키 `val4-index`, `val4-rec-{id}`. 모드·입력값 스냅샷 저장.

### 4.4 TOOL 04 — 켈리 · 분할매수 플래너 (`index_kelly.html`)

**핵심 질문**: "각 종목에 얼마를, 어떤 속도로 살 것인가?" — 사이징(켈리) + 실행(분기 분할매수) + 유지보수(재평가)를 한 도구에 통합.

**진화 이력**: v1 "켈리 포트폴리오 배분기"(단순 정규화 배분) → v2 현재 "분할매수 플래너"(전면 재작성, ~106KB). README는 아직 v1 기술(§8).

**켈리 코어**: 세 시나리오(Bull/Base/Bear)와 세 확률로 `E[log(1+f·r)]` 를 최대화한다. 3-결과에서는 닫힌해가 있다 — `Σ pᵢrᵢ/(1+f·rᵢ)=0` 의 분모를 걷어내면 `α f² + β f + γ = 0` (α=r₁r₂r₃, γ=E[r])이 되고, log 정의역 안의 근이 유일한 최대점이다. Base 를 현재가·확률 0 으로 두면 정확히 구 공식 `f = p/a − q/b` 로 환원된다(§7-D20). 음수 켈리 종목은 배분 제외(§7-D6). 상대 배분은 풀 켈리 정규화(§7-D5). 총노출도 풀 켈리 — 상한 적용 전 주식 노출이 `min(posSum, 1)` 이므로 하프 켈리 안전계수는 폐기됐다(§7-D22). 다만 최종 `equityScale` 은 개별 상한을 자른 뒤의 합이라 **주식 100% 는 켈리가 고르게 퍼졌을 때만 나온다**(§7-D23).

**Method C 보정** (§7-D7): ① 다운사이드 검증 경고 — 하락목표가 현재가의 10% 이내로 얕으면 켈리 출력 신뢰 불가 플래그. ② 개별 포지션 **30% 캡 — 초과분은 채권으로**(§7-D23). 최종 목표비중을 `min(f/Σf × min(Σf,1), MAX_SINGLE)` 로 직접 자르고 그 합이 `equityScale` 이 된다. 종목 수와 무관하게 상한이 걸리므로, 종목이 1~2개인 동안 주식 비중이 자연히 낮게 유지된다(1종목 30% · 2종목 최대 60% · 5종목 균등이면 100%).

**분할매수 실행 계층**:
- 8분기 분할매수 플랜, 분기 순서 강제 — 직전 매수 분기의 바로 다음 분기만 신규 기록 가능. 과거 기록은 값 수정(✎)만 허용, 삭제 후 재입력 불가. *사후확신 편향 차단(§7-D8).*
- **첫 매수 전에는 `startQ`를 현재 분기로 당긴다**(`advanceStartQ`, §7-D26). 매수 기록이 하나라도 있으면 손대지 않는다. 잠금(`startQ` 기준)과 금액 계산(`remainingPace`, `currentQGuess()` 기준)이 같은 분기를 보게 하는 장치다 — 어긋나면 "남은 7칸으로 금액을 나누면서 기록은 지난 Q1 에만 되는" 상태가 되고, 거기 기록하면 `quarter`와 `date`가 서로 다른 분기를 가리키는 항목이 저장된다.
- 자본 흐름: 날짜 있는 납입 이력. 잔여 페이스 재계산은 미래 분기에만 반영, 과거 매수액은 동결.
- 실적 발표일 추적(earnings date) 보조.

**유지보수 계층**:
- **재평가(📊)**: 원장(ledger) 엔트리로 old→new 값 + 근거(필수) 기록, 켈리 재계산 트리거.
- **비활성 감지**: 재평가 후 켈리가 음수가 된 보유 종목 → 배너로 구분 안내: thesis-broken(논리 훼손 → 즉시 전량 매도 권고) vs valuation-based(밸류에이션 원인 → 분할 매도 허용). 계속 보유 시 근거 기록.

**통화**: USD 기본(미국 브로커리지 전제), KRW는 설정 환율로 참고 표시만.
**백업**: export/import JSON (`dataSnapshot`), 백업 상태 바 UI.

**시나리오 필드**: `up`·`base`·`down`(목표가) + `prob`(P(Bull))·`pBase`. `pBear = 1−prob−pBase` 로 도출하며 저장하지 않는다. 입력은 셋 다 받아 합 100%와 `Bull ≥ Base ≥ Bear` 순서를 검증한다. `base` 없는 v4 종목은 켈리를 보류한다.

**후보 파이프라인 계층** (기각 원장):
- `candidates[]`에 **안 산 것들**만 담는다 — `reviewing`(검토중) / `rejected`(기각). 편입되면 `candidates`에서 지우고 `holdings`로 옮기므로 같은 사실이 두 곳에 저장되지 않는다.
- 기각 사유 6종: `factor`(팩터 중복·이미 뚫린 칸) / `circle` / `premium` / `valuation` / `thesis`(자동 등재) / `abandoned`.
- **재검토 트리거 필수** — "PBR 6배 이하로 오면 재검토"처럼 구체적 숫자가 있어야 3년 뒤 검증이 판정 가능하다.
- **3년(1095일) 검증**: 기준일 도달 시 배지 전환 → 현재가·판정·메모 기록. 모달에 *"기각이 틀렸다면 TOOL 03의 r·N 가정을 재검토하세요"* 를 고정 표시한다.
- **기각률** = (thesis 제외 기각) / (thesis 제외 기각 + 도입 이후 편입). `thesis`는 산 적 있는 종목이라 분자·분모에 이중 계상되므로 제외하고, **칸 회수** 지표로 따로 센다. 분모의 "도입 이후"는 `parseInt(h.id,36)`로 생성 시각을 역산해 거른다. UI도 이 구분을 그대로 반영해 **기각**과 **칸 회수**를 별개 섹션(▸ 기각 / ▸ 칸 회수)으로 렌더한다 — 카드 마크업(`rejectCard`)은 공유하되 리스트는 분리되어, 각 헤더 카운트가 그 섹션의 항목 수와 1:1 대응한다.
- **14일 유예**: 기각 등재 후 14일 이내는 삭제·전체 수정 자유, 이후 트리거·메모만 수정 가능. 기준 시각은 `rejectedAt`(등재 시각)이다 — `id`(후보 등록 시각)를 쓰면 오래 검토한 종목이 기각 즉시 잠긴다. 같은 상수로 `rejectedAt`과 `rejectedDate`(사용자가 적은, 소급 가능한 기각일)가 14일 이상 벌어지면 `[소급 등록]` 배지를 단다.
- **칸 회수 강제**: thesis 파기 전량 청산 시 기각 등재 모달이 뜨고, **재검토 트리거를 적어야 닫힌다**(닫기·배경클릭 차단). 등재된 종목은 `livePlanHoldings()`가 걸러내어 **목표 배분·분기 매매일지 양쪽에서 사라지고**, 매매 이력은 `▸ 칸 회수` 카드의 `매매 이력`에서 읽기 전용으로 본다. 같은 이름 재진입은 새 항목으로 강제한다. 등재를 건너뛴 기존 데이터는 자동 변환하지 않고 일지에 `칸 회수 미등재` 배너로 유도한다(§7-D18).
- **검토 시작일은 입력받지 않는다** — 등록 시각으로 자동 기록. 기억 기반 소급 날짜는 부정확하고 진척을 아는 상태에서 유리하게 적힐 수 있다.
- **검토 필드**: `origin`(왜 보나 · 등록 시 선택 입력) / `stage`(능력범위·프리미엄·밸류에이션 수동 체크) / `reviewNote`(작업 메모). 셋 다 선택적이라 스냅샷 버전이 안 바뀐다. 도구별 키 분리(§5) 때문에 TOOL 01/02/03 과 자동 연동은 불가능하며 **수동 자기보고**다. 증거가 아니라 작업 기억이므로 14일 잠금·소급 배지를 걸지 않는다(§7-D19).
- **논지 승계**: 편입 시 `origin`이 `holdings.thesis`로 넘어가 매도 사유 `원 논지 훼손` 판정 때 대조할 원문이 된다 — 칸 회수 모달에 원문이 표시된다. 후보를 안 거친 직접 추가도 종목 추가 폼에서 논지를 받는다. 기존 보유 종목의 소급 입력은 허용하지 않는다(이미 주가를 아는 상태의 사후 서술).

**저장**: 키 `kelly2y` (단일 통합 상태). *구버전 키 `kelly-portfolio`, `kelly-pf-*`는 폐기됨.*

---

## 5. 데이터 모델 — localStorage 키 레지스트리

| 도구 | 프리픽스/키 | 내용 |
|---|---|---|
| 능력범위 | `coc-index`, `coc-record-{id}` | 진단 목록·스냅샷 |
| 프리미엄 | `premiumLab:v1:*`, `premiumLab:hist:*`, `premiumLab:lastTicker` | 평가·이력·최근종목 |
| 밸류에이션 | `val4-index`, `val4-rec-{id}` | 분석 목록·스냅샷 |
| 켈리 플래너 | `kelly2y` | 포트폴리오·원장·후보 파이프라인(`candidates`)·도입일(`rejIntroDate`)·설정 통합 상태 (v5) |
| (폐기) | `kelly-portfolio`, `kelly-pf-index`, `kelly-pf-{id}` | v1 잔재 — 신규 코드에서 사용 금지 |

**규칙**: 새 도구는 반드시 고유 프리픽스를 갖는다. 도구 간 키 공유 금지. 주소(경로)가 바뀌면 localStorage가 분리되므로 파일명 변경은 사실상 데이터 마이그레이션 이벤트다 — 신중히.

---

## 6. 디자인 시스템

| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg` | `#0c0e12` | 배경 (다크) |
| `--panel` / `--panel-2` | `#15181f` / `#1b1f28` | 카드/입력 |
| `--accent` | `#00d4a0` (민트) | 주 액션·결과 |
| `--h1`~`--h5` | 민트/오키드/라임/핑크/시안 | TOOL 04 종목 식별색 — 배분바 세그먼트 + 카드 왼쪽 띠(§7-D24) |
| `--gold` | `#d9a23b` | TOOL 01 계열 |
| 도구별 액센트 | 골드/앰버(#c98f4b)/민트/블루(#6aa9e8) | 허브 카드 구분 |
| 폰트 | Pretendard(본문) + JetBrains Mono 계열(숫자) | 숫자 가독성 |

공통 컴포넌트 패턴: 카드(`.card`), 대형 결과 리드아웃(`.readout` — 도구의 시그니처), 칩(보조 지표), veto 노트(🚫 앰버), 저장 목록 행.

**의미색은 식별색이 아니다.** `--warn`(경고)·`--bad`(위험)·`--good`(양호)·`--cash`(채권)는 **상태**를 뜻한다. 종목·항목을 구분하려고 색을 새로 쓸 때는 이 넷과 색차(ΔE) 33 이상을 확보한다 — 앰버 종목이 "경고 붙은 종목"으로 읽히면 색이 거짓말을 한다. TOOL 04 의 `--h1`~`--h5`가 이 규칙으로 골라졌다(§7-D24).

**VL 베이스 블록** (§7-D13): CSS는 외부 파일로 분리하지 않고(단일 파일·오프라인 원칙 유지), 대신 공유 CSS를 각 파일에 **자구까지 동일한 인라인 블록**으로 표준화한다. 각 `<style>` 최상단의 `/* ===== VL BASE ===== */` 마커가 감싸는 범위 = **공유 토큰(`--bg`~`--mono`) + reset**이며 4개 도구 파일에서 바이트 단위 동일하다. 그 아래 per-tool 하위 섹션에만 도구별 변주를 둔다:
- 액센트: 민트(roe/kelly/hub `--accent:#00d4a0`) vs 골드(circle `--accent:#d9a23b`). 대응 `--accent-dim`·`--accent-ink` 동반.
- 도구 확장: kelly `--cash`/`--cash-dim`, hub `--gold`.
- `.vl-home-btn`(뒤로가기)은 4개 도구에 동일 정의, premium만 자체 스킴(`--border`/`--gold`)으로 대응.

> ⚠️ 토큰을 바꾸면 5개 파일을 각각 수정해야 한다(블록이 동일하므로 복붙은 안전·기계적). 진짜 단일 소스/캐싱을 원하면 외부 파일·빌드가 필요하나 원칙상 기각(§7-D13).

---

## 7. 의사결정 기록 (Decision Log)

| # | 결정 | 기각한 대안 | 이유 |
|---|---|---|---|
| D1 | 체크리스트는 검증된 거장 발언만 수록 | Claude 추론 항목 유지 | 팩트체크에서 오귀속 3건 발견(2분 드릴: 멍거→**린치**, 스커틀벗: 린치→**피셔** 등). 추론 기반 항목 5개 전면 삭제. P1 원칙으로 승격 |
| D2 | 플랫 `index_*.html` 구조 | 폴더 구조(`circle/` 등) | GitHub 웹 UI 운영 단순화. 단점(허브 rename 1회 필요)은 수용 |
| D3 | `localStorage` | `window.storage` | 후자는 Claude 아티팩트 전용 API — GitHub Pages에서 미작동 |
| D4 | Veto(거부권) 로직 | 단순 비율 점수 | 보상적 사고 방지 — 치명 항목(인버전) 미충족은 다른 항목 평균으로 상쇄 불가 |
| D5 | 상대 배분 = 풀 켈리 정규화 | 하프 켈리 정규화 | 수학적 등가(공통 계수는 약분). 하프 켈리는 총노출(주식 vs 현금) 결정에만 의미 — **총노출 부분은 D22 로 대체됨** |
| D6 | 음수 켈리 자동 제외 | 포함 후 수동 판단 | 기대값 음수 종목이 배분을 왜곡 — "이 가격엔 사지 마라"를 구조로 강제 |
| D7 | Kelly Method C (다운사이드 검증 + 40% 캡·비례 재배분) | 무보정 켈리 | 하락목표가 얕으면 켈리가 비현실적으로 공격적인 출력 — 극단값 가드레일 필요. **캡 수치와 재배분 방식은 D23 으로 대체됨**(다운사이드 검증은 그대로) |
| D8 | 분기 순서 잠금(직후 분기만 기록, 과거는 ✎ 수정만) | 자유 입력 | 결과를 알고 소급 기록하는 사후확신 편향 차단 — 추적 데이터의 증거력 보존 |
| D9 | 청산 PBR 기본 1.0 | 낙관적 exit 배수 | 초과수익 소멸(ROE→r)이라는 보수적 하방 기준. 낙관은 사용자가 명시적으로 올려야 함 |
| D10 | 전액 유보(무배당) 가정 | 배당성향 반영 | 모델 단순화 우선. 배당주에는 결과가 낙관적일 수 있음 — 한계로 문서화 |
| D11 | USD 기본 통화, KRW 참고 표시 | KRW 기본 | 미국 브로커리지 계좌가 USD로 운용됨. 환율은 표시용 설정값 |
| D12 | 재평가를 원장 이벤트로 기록(근거 필수) | 값 직접 덮어쓰기 | 판단 변경의 감사 추적(audit trail) — 왜 바꿨는지가 값 자체보다 중요 |
| D13 | CSS 인라인 유지 + 공유 "VL 베이스 블록" 표준화(§6) | 외부 `.css` 파일 분리 | 단일 파일·오프라인·플랫 규칙(P3/P7) 유지. 중복(120~160줄)은 동일 인라인 블록으로 흡수 — 단일 소스·캐싱 이득은 포기(수용). 이 과정에서 circle(`:root` 부재)·premium(토큰명 불일치)의 `.vl-home-btn` var 미해결 버그 동시 수정 |
| D14 | premium을 토큰 통일에서 제외(자체 스킴·세리프·CDN 유지) | 민트 토큰계로 통합 | `--gold`/`--border`/`--muted` 스킴과 Newsreader 세리프는 의도된 정체성(§6). CDN 폰트 의존은 유일한 온라인 예외로 문서화 — 오프라인화는 세리프 정체성 변경을 수반하는 별도 결정 |
| D15 | '1칸' 소모를 정의표로 명문화(아래) | 정의 없이 관행에 맡김 | 정의가 없으면 규율이 아니라 분위기다. 특히 "thesis 파기 후 재진입 = 새 1칸"이 규율의 전부 — "예전에 봤던 회사니까"는 칸을 아껴주지 않는다 |
| D16 | 후보를 `candidates[]` 별도 배열로 | `holdings[]`에 `status:'rejected'` 추가 | `holdings`는 `price`·`up`·`down`·`prob`가 필수인데 Circle 단계나 팩터 중복으로 기각한 후보는 그 숫자를 계산한 적이 없다. 억지로 채우면 거짓 데이터, 비우면 `kellyOf()`·정규화·40% 캡에 `null`이 흘러든다 |
| D17 | 칸 카운터 UI 기각 | "20칸 중 N칸 사용" 표시 | 펀치카드는 자본이 크고 아이디어가 희소해진 후기 버핏의 프레임이다. 성과급이 매년 유입되는 상황과 어긋나므로 20이라는 숫자를 UI에 박으면 이미 기각한 전제를 코드에 고정하게 된다. 추적할 것은 칸 개수가 아니라 **칸의 중복도** |
| D18 | 칸 회수된 종목을 계획에서 제외, 이력은 칸 회수 카드에서 열람 | ① 일지에 `▸ 청산` 섹션으로 남긴다 ② `holdings[]`에서 실제 삭제 | `kellyOf()`가 원장을 보지 않아 전량 청산해도 켈리가 양수로 남는다 → 보유 0주인 칸이 목표 금액을 받고 `posSum`에 끼어 살아있는 종목의 배분까지 희석했다(실측: 남은 종목 목표비중 0.372340 → 0.500000). 표시가 아니라 숫자가 틀린 문제. `holdings` 삭제는 회고용 원장을 잃고 `candidates` 스키마에 원장을 담을 자리가 없으므로, **계획에서만 빼고 배열은 유지**한다. 대가: 청산 후에는 그 종목의 기존 기록을 앱 안에서 수정할 수 없다(백업 JSON 편집으로만 가능) |
| D19 | 검토 필드(`origin`·`stage`·`reviewNote`)는 작업 기억이라 잠금을 안 걸고, 기각 시 `stage`·`reviewNote`를 버린다 | ① 전부 보존 — 아무도 안 읽는 죽은 데이터 ② 기각 카드에 계속 표시 — 카드 비대 ③ 기각 원장처럼 14일 잠금 — 증거가 아니라 작업 메모라 과한 규율 | 기각 원장은 3년 뒤 반증을 위한 증거지만 검토 필드는 검토 중에 쓰는 메모다. 남길 가치가 있는 내용은 기각 메모(`rj-note`)에 옮겨 적으면 되고, 그게 그 필드의 용도다. `origin`만은 3년 검증의 맥락이므로 기각 카드에 남긴다. 편입 시에도 `origin`만 `holdings.thesis`로 승계하고 나머지는 버려 대칭을 맞춘다. 되돌리기는 렌더만 걷어내면 된다 — 필드가 전부 선택적이라 기존 데이터가 그대로 살아 있다 |
| D20 | 켈리를 3-시나리오(Bull/Base/Bear)로 확장, 마이그레이션은 기본값을 채우지 않음 | ① Base 를 선택 입력으로 — 두 계산 경로가 공존 ② 현재가·확률 0 으로 자동 채움 — 무변경이지만 판단을 건너뛰게 됨 ③ 상단·하단의 기하평균 — 아무도 생각한 적 없는 숫자가 배분을 움직임 ④ N-시나리오 일반화 — 4개부터 닫힌해가 사라짐 | 밸류에이션 분석이 Bear/Base/Bull 세 적정가로 끝나는데 입력이 둘이면 기준선이나 하방을 버려야 했다. 마이그레이션 직후 전 종목 켈리가 보류되는 것은 결함이 아니라 forcing function이다 — 빈 칸은 "아직 판단하지 않았다"는 정직한 상태다. 대가: Base 를 다 채울 때까지 목표 배분·매매일지가 빈다. 전제의 한계(기간 일관성·꼬리 절단·상관관계)는 설계 문서 «켈리 전제와 한계» 참조 |
| D21 | 스키마 변경 시 **필드 추가**와 **기존 필드의 의미 변경**을 구분한다. 후자는 스냅샷 버전으로 방어되지 않으므로 배포 전에 롤백 절차를 문서화한다 | ① 버전 가드로 막는다 — 옛 빌드는 이미 배포돼 소급 적용 불가하고, 새 빌드에 넣어봐야 미래 다운그레이드만 막는다 ② 필드를 새로 만들어 의미 변경을 피한다 — 이번 건은 `prob` 재사용이 마이그레이션을 불필요하게 만든 이점이 더 컸다 | D20 에서 `prob` 의 의미가 승률 → P(Bull) 로 바뀌었다. 이름·타입이 같아 **옛 빌드가 거부하지 않고 그냥 읽는다** — 실측: 현재가100·Bull150(25%)·Base120(50%)·Bear70(25%) 종목이 새 빌드 f=+1.6085(배분 포함), 옛 빌드 f=−0.6667(음수 켈리로 제외). 에러 없이 종목이 계획에서 사라진다. D18 까지의 변경은 전부 순수 추가라 롤백이 데이터-안전했으나 D20 부터는 아니다. **장래에 버전 가드를 만든다면 '거부'가 아니라 '경고 후 계속'이어야 한다** — 오프라인 단일파일 PWA라 거부하면 사용자가 포트폴리오에 아예 접근하지 못한다 |
| D22 | 총노출도 풀 켈리 — `equityScale = min(posSum, 1)`, 하프 켈리 계수 폐기 | ① 하프 켈리 유지(D5) ② 0.75 같은 중간 계수 ③ 계수를 설정값으로 노출 | 사용자가 "주식 목표 50% 상한"을 도구의 안전장치가 아니라 켈리 계산 결과로 오해하고 있었다. 오해를 풀고 나서 풀 켈리를 택했다 — 애초에 시나리오·확률을 보수적으로 잡는 것으로 안전마진을 두겠다는 판단이다. 계수를 설정값으로 빼는 건 P6(과공학) 기각. **대가 두 가지**: ⑴ 켈리 합이 1 을 넘으면 채권이 0 이 되어 8분기 분할매수의 "매수 대기 실탄"이 사라진다 — 분할은 남은 분기 수로만 통제된다. ⑵ **보유 종목이 2개 이하면 40% 개별 상한이 작동하지 않는다** — 상한 재분배는 `relCap × 종목수 ≥ 1` 일 때만 실행되는데(`compute()`), `equityScale=1` 에서 `relCap = 0.40` 이라 2종목이면 `0.8 < 1` 로 건너뛴다. 실측: 2종목일 때 한 종목이 64.10%, 3종목이면 정상적으로 40% 에서 잘린다. 하프 켈리에서는 `relCap = 0.80` 이라 2종목에서도 상한이 걸렸다 |
| D23 | 개별 상한 40%→**30%**, 초과분을 미캡 종목이 아니라 **채권**으로 | ① 상한 수치만 조정하고 재배분 유지 — 구멍이 그대로 남는다 ② 상한 20%(=5종목 균등비중) — 켈리가 균등배분으로 축퇴해 확신 배율 1.0배 ③ 상한 폐지 — D7 을 통째로 뒤집는다 ④ 종목 수가 적을 때만 별도 규칙 — 경계에서 배분이 튄다 | 구 재배분은 `relCap × 종목수 ≥ 1` 일 때만 실행돼 **종목이 1~2개면 상한이 통째로 건너뛰어졌다**(실측: 2종목에서 한 종목 87%, 1종목이면 100%). 상한을 조일수록 이 구멍이 넓어지는 역설도 있었다(20% 상한이면 N≤4 에서 미작동). 피셔식 검토는 종목당 수개월이라 **N 이 작은 기간이 오래 간다** — 하필 가장 덜 분산된 구간에서 상한이 없었다. 초과분을 채권으로 보내면 재배분 루프와 N 조건이 모두 사라지고 상한이 모든 N 에서 걸린다. 30% 는 5종목 균등비중(20%)의 1.5배 — 켈리가 확신 차이를 표현할 여지를 남기면서 1종목 30%·2종목 60% 로 초기 노출을 묶는다. **대가**: 켈리가 쏠린 배치에서는 5종목을 채워도 주식이 100% 에 못 미친다(실측: 쏠린 5종목 67%). 미캡 종목이 초과분을 넘겨받아 켈리보다 많이 받던 것도 사라진다 — 각 종목은 자기 켈리 몫만 받는다. `rel = w/equityScale` 로 두어 `rel × equityScale = 최종비중` 항등식을 유지했으므로 렌더 4곳은 안 고쳤다 |
| D24 | 종목 식별색 `--h1`~`--h5` 를 배분바와 카드에 함께 쓰고, **색은 종목 id 로 매핑** | ① 구분선만 — 칸이 나뉜 것까지만 알 수 있고 어느 종목인지는 모름 ② 액센트 명도 사다리 — 글자(#04140f) 가독성 때문에 #00a87f 보다 어두울 수 없어 인접 대비가 1.14~1.16:1 에 그침(측정) ③ 바 인덱스로 색 매기기 — 켈리 보류·음수 종목이 끼면 카드 색이 통째로 밀림 ④ 앰버·빨강 포함한 6색 — `--warn`·`--bad` 와 충돌 | 배분바 세그먼트가 전부 `--accent` 라 종목이 인접하면 한 덩어리로 보였다. 게다가 라벨은 `w>8%` 일 때만 나오므로 켈리가 쏠리면(실측 `30/5.3/5.3/5.3/5.3`) 네 칸이 이름 없는 조각이 된다 — **색이 라벨을 대신한다.** 폰 360px 기준 바 내부 330px 에서 5.3% 칸은 색 폭 15.5px 로 또렷하다(1% 미만은 구분선이 먹어 사라지지만 카드에 이름·금액·%가 다 있어 정보 손실은 없다). 색 선정 기준 셋: 글자 대비 7.3:1 이상 · 서로 ΔE 38 이상 · 의미색과 ΔE 33 이상(§6). 순서도 인접 색차가 최대가 되게 배열했다(최소 ΔE 38.3 → 87.8). **id 매핑이 핵심이다** — 바는 `pos`(켈리 양수만), 카드는 `rows`(보류·음수 포함)를 돌아 인덱스가 어긋난다. 색이 밀려도 예외가 안 나고 화면만 조용히 거짓말하므로 자체 검사 4건으로 고정했고, 인덱스 매핑으로 되돌리는 돌연변이가 그중 2건을 FAIL 시키는 것까지 확인했다 | **대가**: 6종목부터 색이 순환한다(목표가 5개라 수용, 구분선이 인접 구분을 계속 맡는다). 종목을 팔면 뒤 종목의 색이 한 칸씩 당겨진다 — 색은 종목의 영구 정체성이 아니라 현재 배분 안에서의 자리다 |
| D25 | `--demo` 보유 종목을 고정 픽스처로 못박음 (TSMC·Costco·Visa·ASML·Novo·Adyen) | ① 매번 즉석 데이터 — 화면 비교의 기준이 없어 '전에는 어땠더라'를 못 함 ② 실데이터 복사 — 저장소에 개인 포지션이 들어감 | 한 화면에서 **5색 · 개별 상한(TSMC 30% 걸림) · 채권 8.6% · 켈리 보류(Adyen, Base 미입력)** 가 전부 렌더되도록 값을 맞췄다. 값이 바뀌면 화면 비교의 기준이 사라지므로 `build-selfcheck.js` 주석에 기대 배분표를 함께 박아 두었다 — 표와 실제가 어긋나면 둘 중 하나가 틀린 것이다. 후보 이름은 보유 종목과 겹치지 않게 골랐다(같은 이름이 보유이자 기각으로 보이면 파이프라인 의미가 흐려진다) |
| D26 | 첫 매수 전이고 시작 분기가 지났으면 `startQ`를 **현재 분기로 당기고 저장한다** | ① 현재 분기를 열되 `startQ`는 보존 — 헤더의 `2026Q1 시작`과 Q2부터인 일정이 서로 다른 말을 한다 ② 잠금 문구만 '시작 분기가 지났습니다 · ✎ 수정에서 옮기세요'로 안내 — 불일치는 그대로 남고 매번 수동 ③ 그대로 둔다 — 없던 과거 매수를 지어내게 만든다 | **증상**: 2026-08월에 `startQ=2026Q1`인 종목의 일지가 Q1만 열려 있고 Q2가 `🔒 첫 매수 후 실적일 기준으로 열림`이었다. **근본 원인**: 일지 잠금(`render`)은 `h.startQ`를, 금액 계산(`remainingPace`)은 `currentQGuess()`를 봤다 — 같은 화면에서 **금액은 남은 7칸으로 나누면서 기록은 Q1에만** 할 수 있었다(실측). 거기 기록하면 `{quarter:'2026Q1', date:'2026-08-13'}` 이라는 모순된 원장 항목이 저장되고(첫 매수는 날짜가 오늘로 프리필된다), 그 뒤 `firstBuyIdx=Q1`이 되어 Q2가 실적일 경과로 즉시 열려 8분기 계획이 시작하자마자 두 칸을 잃는다. D8(사후확신 편향 차단)이 막으려던 것을 잠금이 **오히려 강요**하고 있었다. 아직 시작하지 않은 계획에는 왜곡할 과거가 없으므로 당기는 것이 안전하고, `remainingPace`가 이미 그렇게 세고 있었으니 새 규칙이 아니라 **일치**다. **대가**: "Q1에 사려다 안 샀다"는 사실이 조용히 지워진다(사용자가 이 대가를 알고 선택). 8분기 창이 통째로 밀려 끝 분기도 바뀐다. **검증**: `advanceStartQ` 단위 11건 + 자체 검사 4건. 자체 검사는 `localStorage`에 지난 `startQ`를 심고 **`load()`를 실제로 호출**한다 — `load()`에서 호출만 빼는 돌연변이에서 자체 검사 3건이 FAIL하는 동안 `test_pipeline.js`는 129건 전부 통과했다. 단위 검사만으로는 못 잡는 자리다 |

**D15 부속 — '1칸' 소모 정의표**

| 행위 | 칸 소모 |
|---|---|
| 새 기업 최초 편입 | **1칸** |
| 8분기 분할 매수 (같은 종목) | 0칸 — 1칸 안의 절차 |
| 밸류에이션 기반 분할 매도 후 재매수 | 0칸 — thesis 유지 중 |
| **thesis 파기 후 청산 → 나중에 재진입** | **새 1칸** |
| 매크로 ETF 바스켓 (예: 구리 4종) | 테마당 1칸, 종목당 아님 |

칸이 뚫리는 순간은 1차 매수 시점이 아니라 **thesis를 확정한 순간**이다. 따라서 분할매수 도중 tranche를 건너뛰는 것은 **칸을 회수하는 행위**다. 원본 펀치카드는 되돌릴 수 없지만 이 시스템은 8분기 동안 되돌릴 수 있다 — 회수를 한 번도 하지 않으면 그 우위가 소멸한다.

---

## 8. 알려진 한계 · Non-goals · 드리프트

**한계**
- 실시간 시세 없음 — 가격·목표가는 수동 입력 (GIGO 명시)
- 기기 간 동기화 없음 — localStorage는 기기·브라우저별 분리, 백업은 JSON export로 수동
- 전액 유보 가정 — 배당 기업에는 밸류에이션이 낙관 편향
- 확률(p)·목표가는 사용자 추정치 — 켈리 출력의 신뢰도는 입력의 신뢰도를 넘지 못함

**Non-goals** (의도적으로 안 하는 것): 서버·계정·클라우드 동기화, 실시간 데이터 연동, 자동매매, 다중 사용자.

**문서 드리프트 (2026-07-10 기준)**: `README.md`가 v1 켈리("포트폴리오 배분기, 하프 켈리 포함")를 기술 중 — 실제 배포는 v2 "분할매수 플래너". README의 "index_main.html rename" 안내도 이미 완료된 과거 절차. **README 갱신 필요.**

§4.4의 *"과거 기록은 값 수정(✎)만 허용, 삭제 후 재입력 불가"* 는 **코드와 어긋난다.** `index_kelly.html:1329, 1338, 1354`에서 모든 원장 항목에 ✕ 버튼이 조건 없이 렌더되고 `1457-1461` 핸들러는 확인창도 없이 삭제한다. D8이 실제로 강제하는 것은 **분기 순서**뿐이다. 후보 파이프라인의 14일 유예(§4.4)는 이 기존 정책과의 일관성이 아니라 기각 원장에 특별히 필요한 증거력을 근거로 도입된 것이다.

**후보 파이프라인 관련 추가 기록**:
- 브라우저 실기 검증 미실시. 지금까지의 확인은 Node 문법 파싱(`new Function()`)·순수함수 회귀 테스트(`test_pipeline.js`)·코드 리딩뿐이며, 실제 DOM에서 렌더링·클릭까지 확인한 적은 없다.
- `todayStr()`(`index_kelly.html:1716`)은 `new Date().toISOString()` 기반이라 UTC 자정 기준이다 — KST 00:00~08:59 사이에 후보를 등록하면 하루 전 날짜로 기록된다. 파일 내 기존 호출 5곳(`998, 1940, 1962, 2288, 2325`)과 동일한 패턴이라 새 코드만 KST로 바꾸면 날짜 계산이 오히려 악화된다 — `YYYY-MM-DD` 문자열의 `Date.parse`는 UTC 자정으로 해석되므로, 한쪽만 시간대를 바꾸면 두 값이 어긋난다.

---

## 9. 백로그 후보 (확정 아님)

- README를 현재 코드 기준으로 갱신 (§8 드리프트 해소)
- 프리미엄 체크리스트 11요소의 원저 서지 정보 확정
- 켈리 플래너 v1 폐기 키(`kelly-pf-*`) 마이그레이션/정리 유틸
- 도구 간 종목 컨텍스트 연결(예: TOOL 01 통과 종목을 TOOL 03에 프리필) — P6(과공학 경계)에 따라 실사용 필요 확인 후
- **후보 파이프라인 기각 조건 점검 (2029-08 예정)** — 도입 3년 시점에 ① 기각 건 0 ② 기각률 50% 미만 ③ 검토중 항목이 전부 1년 이상 미결 ④ 3년 검증 완료 0 중 하나라도 해당하면 이 기능은 규율이 아니라 사후 서사로 쓰이고 있는 것이므로 재검토한다. 검토 필드(§7-D19)는 별도로 ⑤ 체크박스가 전 항목에서 등록 시 상태 그대로 ⑥ `origin`이 절반 이상 빔 ⑦ `reviewNote`가 한 번도 안 쓰임 — 이 중 하나면 장식이므로 렌더를 걷어낸다
- **켈리 3-시나리오 점검** — ① Base 와 Bull 이 늘 같은 값으로 적힌다 ② 확률 합 맞추기가 번거로워 입력을 회피한다 ③ 마이그레이션 후 한 분기 안에 Base 를 다 못 채운다 ④ 종목마다 도달 기간 관점이 제각각이라 f 비교가 무의미해진다 — 하나라도 해당하면 재검토한다. ④는 §7-D5(켈리를 상대 배분에 쓰는 결정)까지 소급된다

---

*이 문서는 배포 코드와 개발 대화의 의사결정을 근거로 작성됨. 코드가 진실의 원천(source of truth)이며, 충돌 시 코드 기준으로 이 문서를 갱신한다.*
