# jhw_ValuationLab — 프로젝트 가이드 (Claude)

가치투자 의사결정 파이프라인을 4개의 독립 웹 도구로 구현한 **개인용 분석 스위트**.
서버리스 · 단일 파일 HTML · vanilla JS · 모바일 우선. GitHub Pages 배포(`salt99.github.io/jhw_ValuationLab/`).

## 먼저 읽을 것 (Source of Truth)

- **`docs/HANDOFF.md`** — **세션 시작 시 가장 먼저 읽는다.** 직전 세션이 남긴 인수인계.
  진행 중인 일, 손대지 말 것, 되돌리는 법. 항상 이 한 파일뿐이다.
- **`PRD.md`** — 설계 의도·원칙·의사결정 기록의 정본. 작업 전 반드시 정독. 코드와 충돌 시 **코드가 진실**이며 PRD를 갱신한다.
- **`README.md`** — 퀵스타트. ⚠️ 현재 v1 켈리 기준으로 **드리프트**됨(PRD §8). 신뢰하지 말고 코드/PRD 기준으로 판단.

## 파일 구조 (플랫 규칙 — PRD §7-D2)

| 파일 | 도구 |
|---|---|
| `index.html` | 허브 (GitHub Pages 진입점) |
| `index_circle.html` | TOOL 01 · 능력범위 체크리스트 |
| `index_premium.html` | TOOL 02 · 기업 프리미엄 체크리스트 |
| `index_roe.html` | TOOL 03 · 밸류에이션 4-모드 분석기 |
| `index_kelly.html` | TOOL 04 · 켈리 · 분할매수 플래너 |

- 모든 도구는 저장소 루트의 `index_{name}.html`. **폴더 구조 금지.**
- 새 도구 추가: `index_{name}.html` 생성 → 허브(`index.html`)에 카드 추가(TOOL 0N·액센트색) → PRD §5 키 레지스트리 등록.

## 반드시 지킬 원칙 (PRD §2)

- **P1 출처 검증 (non-negotiable)**: 체크리스트 항목은 거장이 실제로 말한 것으로 검증된 것만. 추론·응용 항목은 "구성" 명시 또는 삭제. *과거 오귀속 3건 발견 이력 있음(§7-D1).*
- **P2 로컬 우선**: 서버·로그인·외부 API 없음. 상태는 전부 `localStorage`, **도구별 고유 프리픽스 필수, 키 공유 금지**(§5). 파일명 변경 = 데이터 마이그레이션 이벤트(경로가 바뀌면 localStorage 분리).
- **P4 사고 보조 ≠ 매매 지시**: 모든 도구에 가정·한계 디스클레이머를 화면 안에 명시. GIGO.
- **P5 검증 후 배포**: 수식·역산 로직은 배포 전 Node.js(DOM-stub)로 단위 검증. 손계산 값과 코드 출력 일치 확인.
- **P6 과공학 경계**: 실사용에서 필요가 확인된 기능만. "있으면 좋은" 기능은 기각.

## 작업 규칙

- **의존성 없음**: 빌드·패키지 매니저 없음. 파일을 브라우저에서 직접 열어 확인. 외부 CDN/프레임워크 추가 지양(오프라인·단일파일 원칙).
- **디자인 토큰 유지**: 다크 테마, 도구별 액센트색(골드/앰버/민트/블루), Pretendard+Mono. PRD §6 참고.
- **수식 변경 시**: 반드시 단위 검증 스크립트로 확인 후 커밋(P5). 밸류에이션·켈리 공식은 PRD §4에 정본.
- **커밋**: 사용자가 요청할 때만. 메시지 형식은 아래 **커밋 컨벤션** 섹션을 따른다.
- **PRD 유지**: 도구 추가·개편 시 PRD §4(명세)·§5(키)·§7(결정 기록) 갱신.

## 작업 기록 — SPIKE · DEVLOG · ADR · HANDOFF

네 가지는 **답하는 질문이 다르다.** 겹치면 어느 것도 안 읽히게 된다.

| | 질문 | 위치 | 시점 |
|---|---|---|---|
| **SPIKE** | 이걸 해야 하나? 되긴 하나? | `docs/spike/YYYY-MM-DD-{주제}.md` | 만들기 **전** |
| **ADR** | 왜 이렇게 정했나? | **PRD §7 결정 기록** (D1, D2, …) | 정한 **순간** |
| **DEVLOG** | 뭘 했고 뭘 배웠나? | `docs/devlog/YYYY-MM-DD-{주제}.md` | 끝난 **후** |
| **HANDOFF** | 다음 사람이 뭘 알아야 하나? | `docs/HANDOFF.md` (한 파일, 덮어씀) | 세션 **경계** |

### SPIKE — 시간 상자를 건 조사

착수 여부가 불확실하거나 전제를 검증해야 할 때. **반드시 판정으로 끝난다** — 채택 / 부분 채택 / 기각.
판정 없이 끝난 spike는 조사가 아니라 배회다. 기각 조건을 함께 적으면 나중에 되돌아볼 수 있다.

### ADR — PRD §7을 그대로 쓴다

**새 디렉터리를 만들지 않는다.** PRD §7이 이미 ADR이다: 표 한 줄에 결정·기각한 대안·이유.
D1~D17이 그렇게 쌓였고 코드 주석과 문서가 `§7-D2` 식으로 참조한다.

한 줄로 안 되는 결정만 `docs/adr/` 에 파일로 빼고, §7에는 그 파일을 가리키는 한 줄을 남긴다.
정본은 언제나 §7이다.

### DEVLOG — 배운 것이 본론

무엇을 했는지는 `git log`에 있다. DEVLOG가 담을 것은 **git이 못 담는 것**이다:
막혔던 지점, 틀린 접근, 사용자 판단이 필요했던 갈림길, 다음에 안 되풀이할 실수.
"무엇을" 섹션은 짧게, "배운 것" 섹션을 길게.

### HANDOFF — 세션 경계

- **세션 시작**: 이 파일부터 읽는다.
- **세션 종료**: 덮어쓴다. 항상 한 파일이다 — 여러 개면 "최신이 뭐지"를 매번 찾아야 한다.
  지난 내용은 `git log docs/HANDOFF.md` 에 남는다.

담을 것: 지금 상태 · 진행 중이라 이어받을 것 · 알아야 할 함정 · **손대지 말 것과 그 이유** ·
되돌리는 법. 손대지 말 것이 특히 중요하다 — 이유를 모르면 다음 세션이 "개선"이라며 되돌린다.

### 자동화 (훅)

문서 규약은 안 지키면 그만이라 두 가지를 훅으로 강제한다 — `.claude/settings.json`.

| 훅 | 스크립트 | 동작 |
|---|---|---|
| `SessionStart` | `.claude/hooks/handoff-inject.sh` | `docs/HANDOFF.md` 를 세션 컨텍스트에 자동 주입. 파일이 없으면 조용히 통과 |
| `Stop` | `.claude/hooks/handoff-remind.sh` | HANDOFF 가 커밋 3개 이상 뒤처지면 **세션당 한 번** 알림. 차단하지 않음 |

`Stop` 은 매 턴 끝마다 발화하므로 두 겹으로 조용하게 만들었다 — 실제로 뒤처졌을 때만, 그리고
세션당 한 번만(`$TMPDIR/claude-handoff-nag-{session_id}` 마커). 임계값은 스크립트 상단
`THRESHOLD`. 훅을 끄거나 확인하려면 `/hooks`.

### 언제 쓰나

- 한 세션짜리 잡무 → 아무것도 안 쓴다. 커밋 메시지로 충분하다.
- 여러 세션에 걸치거나 되돌아볼 판단이 있었다 → DEVLOG
- 착수 전에 전제를 검증했다 → SPIKE
- 세션을 끝낼 때 미결이 있다 → HANDOFF 갱신 (미결이 없으면 "미결 없음"이라고 쓴다)

## 알려진 백로그 (PRD §9, 확정 아님)

- 프리미엄 11요소 원저 서지 확정
- 켈리 v1 폐기 키(`kelly-pf-*`) 정리 유틸


## 커밋 컨벤션

- **Conventional Commits 형식**을 따른다: `<타입>(<범위>): <제목>`
- **타입**: `feat`(기능 추가) · `fix`(버그 수정) · `docs`(문서) · `style`(포맷·공백 등 동작 무관) · `refactor`(리팩터링) · `perf`(성능) · `test`(테스트) · `build`(빌드) · `ci`(CI) · `chore`(잡무) · `revert`(되돌리기)
- **제목**: 명령형(예: `add`, `fix`), 최대 50자, 끝에 마침표 없음
- **본문**: *무엇을* 했는지가 아니라 *왜* 바꿨는지 설명
- **이슈 참조**: 푸터에 `Refs: #123` 형식으로 표기
- 논리적으로 구분되는 변경은 **각각 별도 커밋으로 분리**
- 커밋 메시지에 **Claude/AI 저작 표기(푸터) 미첨부**
